Run the full test suite and report results clearly.

1. Run `pytest -v`
2. Run `ruff check src/`
3. Summarize: total tests, passed, failed, errors
4. If failures exist, show the failing test names and brief error descriptions
