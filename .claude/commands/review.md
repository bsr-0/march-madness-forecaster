Review the current changes using the code-reviewer skill. Compare against the original plan or requirements.

1. Run `git diff` to see what changed
2. Run `pytest -v` and `ruff check src/` to verify current state
3. Apply the code-reviewer skill: check plan alignment, code quality, architecture, and categorize issues as Critical/Important/Suggestions
4. Present structured review output
