# COUNCIL_LESSONS.md

Consolidated digest of 25 LLM-council sessions run 2026-03-31 → 2026-04-12.
Raw transcripts and HTML reports were deleted 2026-04-13 after the 2026
tournament; this file is the surviving record. New sessions will append below.

**Source-of-truth precedence** (per MEMORY.md): `pipeline_freeze.json` > this
file > audit `.md` files > code comments. Where a settled decision from these
sessions is indexed in MEMORY.md, MEMORY wins.

---

## 1. Lessons Learned (cross-cutting)

Patterns that recurred across multiple sessions. Keep reading these before
proposing new work — most "new ideas" have been tried or ruled out.

### Modeling
- **BSS ≈ 0 vs seed baseline is ground truth, not a bug.** 17 yrs × every
  tested model (logistic, LightGBM, XGBoost, 27-feature ensemble, stacking,
  GNN, transformer) all BSS ≈ 0. Seeds already encode the signal available in
  tournament-history features. `[locked; MEMORY.md §2 D1–D4, D11]`
- **7-feature logistic is the ceiling given data constraints.** GNN,
  transformer, ensemble stacking, learned feature-selection are all
  overambitious for 60-ish holdout games. `[locked; MEMORY.md §1]`
- **Training on regular season + tournament is a distribution mismatch, not a
  sample-size fix.** Adding NIT / conference tournaments was considered and
  rejected: structurally similar ≠ statistically similar; 2000× more
  irrelevant samples bury tournament signal.
- **Tournament-only model vs blended stacked model** was never directly
  measured head-to-head. Flagged as the diagnostic experiment but not run.
  `[closed 2026-04-13; §2 O7]`
- **Calibration on 60–70 holdout games is fatal.** Temperature scaling on that
  sample produces "noise with decimal places." Vegas lines proposed as a
  better-calibrated external source.

### Pool strategy (post-pivot, locked 2026-04-02)
- **Stop optimizing prediction accuracy. Optimize bracket-pool EV against
  opponents.** Unanimous 5-advisor agreement. `[locked; MEMORY.md §1]`
- **Optimize P(1st), not EV.** EV ranking failed on 2026: the system produced
  a 1440-pt winning bracket (4/4 F4, det_champ96) but ranked it #11 in its
  own portfolio. Ranking failure, not prediction failure.
- **Recommended mode: `champ_first_tv`.** Aggressive alt: `e8_first_tv`.
  `[locked; MEMORY.md §1]`
- **Chalk bias is measured but champion-weight fading doesn't improve pool wins** (F4: public picks 1-seeds 42% vs true ~21%). Empirical table built (G1). Champion-weight fade implemented (G2). G3 FAIL: P(1st) collapsed 14× vs `champ_first_tv` (0.04% vs 0.56%). The public over-pick signal is real but fading it trades tail upside for average-bracket diversity — wrong direction for winner-take-all. `[closed as dead-end D16 on 2026-04-15; §2 O23]`
- **Stochastic bracket generation is non-negotiable.** Argmax collapses
  probabilistic models into crowd-following; the contrarian signal lives in
  the probabilities, not the modal bracket.
- **Opponent model is NOT the load-bearing wall (reversed 2026-04-13).**
  Three 2026-04-12 sessions flagged it as such. 3 rounds of investigation
  closed the whole branch: O1 (data already existed), O3 (ranking has
  signal, mean ρ = +0.37), O4 (independence holds, pooled z = −4.15),
  O10 (no copula needed), **O21 (pool-history blending does not change
  bracket rankings — null result across 2024-2025)**. The opportunity,
  if any remains, is in the **base model** (game-outcome probabilities),
  not opponent modeling. `[see §2 O1/O3/O4/O10/O21; ANALYSIS_O4 and
  ANALYSIS_O21 for evidence]`
- **Opponent-model marginals affect absolute P(1st) but not bracket
  *ranking*.** Spearman ρ between predicted P(1st) and actual score is
  flat across opponent-model-weight settings w ∈ {0, 0.25, 0.5}, and
  degrades slightly at w ≥ 0.75. Implication: the choice of opponent
  model is irrelevant to "which bracket should I submit?" — it only
  affects "how likely is my bracket to win?" Future opponent-model
  tuning should be framed around calibration, not ranking. `[added
  2026-04-13; ANALYSIS_O21_MARGINAL_BLEND.md]`
- **Check opponent-model marginals before correlations.** The 2026-04-12c
  council diagnosed opponent-model validity as a correlation problem
  without first measuring correlation or marginals. 4-year analysis
  (`ANALYSIS_O4_OPPONENT_CORRELATION.md`) found correlation is fine;
  marginals are off by 5pp (mean absolute) vs ESPN national, up to 18pp
  on individual teams. But see O21: the marginal divergence doesn't
  translate to ranking improvement. Future "opponent model is wrong"
  diagnoses should start with marginals — cheaper and was the actual
  issue, even though it didn't move the needle here. `[added 2026-04-13]`
- **Opponent weights 60 % ESPN picks / 30 % Massey / 10 % seed fallback.**
  `[locked 2026-04-12; MEMORY.md §1]`
- **F4 cap at 2, not 1.5.** Tuning the cap from a 14-yr historical average is
  small-sample overfitting.
- **Pool size N ≠ usable opponent count K.** `N` is the raw pool size
  (e.g. 31 for 2026). The user's own bracket is correctly excluded from
  the opponent model to avoid self-reference, so `K = N − 1` (30 for
  2026). Verified across all pool-history years: 2023 = 18/19,
  2025 = 32/33, 2026 = 30/31 (2024 = 25/25 is the outlier — pre-dated the
  user's entry). This is documented here because the 2026-04-12c council
  flagged "collect 31 brackets" as the top priority without knowing the
  30 already existed on disk, scraped earlier that same day. Future
  "collect real pool brackets" framings should specify `K` explicitly.
  `[added 2026-04-13]`

### Validation / statistics
- **14–17-year backtests are underpowered (~9–16 % power).** Use them for
  binary/directional claims (e.g. "P(1st) beats EV") — do NOT use them to
  tune parameter values (F4 cap, MC sim count, blend weights).
- **Validate before shipping. Formula-correctness is not validation.** The
  barthag r = 0.73 divergence (reverse-engineered from ESPN box scores vs.
  real Torvik) is the precedent to remember. Local four factors still need
  the same validation: gate ≥ 0.99 per-season, no systematic residual bias.
  `[closed 2026-04-13; §2 O2]`
- **Silent failures are worse than crashes.** "Won't crash" ≠ "works".
  Team-name matching bug (snake_case double-underscores → silent fallback to
  seed rates) was load-bearing before it was caught. Runtime guards beat
  documentation.
- **Stacking contamination was the validity crisis before the pivot.** Meta-
  learner trained on the LOYO predictions used to evaluate it. The pivot made
  it moot (stacking disabled) but the governance lesson stands: no
  revoke-and-relock protocol existed.
- **Researcher degrees-of-freedom leakage** (hyperparameters tuned while
  observing backtest on same years) was raised but never audited.
  `[closed 2026-04-14; §2 O17]`
- **Define kill-criteria BEFORE running tests.** Without a pre-committed
  threshold you rationalize whatever number comes back.
- **Held-out last-3-years validation is recommended but not enforced.**
  Multiple sessions asked for it; no gating mechanism exists.
  `[closed 2026-04-13; §2 O20 — gate is the single-year holdout [2025]
  per MEMORY.md §1]`

### Data pipeline / infrastructure
- **Trank.php promoted to primary data source over cbbdata.com** (decided
  2026-04-06). Rationale: server-side date filtering is a second independent
  leakage guard. Chooses loudly-failing Cloudflare fragility over silently-
  corrupting hardcoded `TOURNAMENT_START_DATES` dict.
- **`TOURNAMENT_START_DATES` dict is a single point of failure** — hardcoded,
  requires yearly update, no test catches a missing entry. `[closed 2026-04-13; §2 O16]`
- **`_validate_pretournament()` runtime guard added to
  `noseed_model._load_team_stats()`** with 10 regression tests (2026-04-05).
  Still need to trace the full call graph to confirm coverage on all load
  sites. `[mostly closed]`
- **Response hashing + artifact provenance tracking landed at the manifest
  layer.** Every `data/raw/manifest_{year}.json` now carries a
  `provenance.data_artifacts` ledger keyed by the manifest's logical
  artifact name, with sha256 (full + 16-char short), bytes, mtime_utc,
  exists flag, and provider. Utility `src/data/data_provenance.py`
  reuses `manifest_generator._sha256_file`; idempotent. Refresh:
  `python scripts/audit_data_provenance.py`. Drift guard:
  `tests/data_integrity/test_data_provenance_audit.py` (14 tests:
  contract + drift detection + idempotency + sentinel handling +
  coverage). Production manifests 2025 (3 artifacts) and 2026 (7
  artifacts) populated 2026-04-15. **Request logging (URL / fetch
  time / response headers at scrape time) is the deferred half** —
  requires touching every scraper (BartTorvikScraper,
  TournamentResultsScraper, ESPNPicksScraper, …) and is independent of
  what landed here. `[closed 2026-04-15 (response-hash + manifest-ledger);
  request-logging deferred — see §3 row 50]`
- **Pre-2011 vs post-2011 tournaments aren't interchangeable.** Field expanded
  64 → 68, analytics culture shifted. Naive concatenation of 19 yrs of picks
  ignores non-stationarity. `[closed 2026-04-14; §2 O19]`

### Diversity / optimizer
- **Diversity collapse is two observations, not one** (diagnosed 2026-04-15).
  **Observation A** — `ParetoOptimizer.generate_pareto_brackets` returns 5–8
  unique brackets in production reports (up to 13 for `det_*` construction
  paths). This is INTENDED: the risk sweep honestly reflects the
  (probability × differentiation) scoring curve, which genuinely has few
  crossovers when one team is an undervalued favorite (Michigan 2026,
  Gonzaga 2017). Opt-in remediation exists:
  `ParetoOptimizer.generate_pareto_brackets(include_champion_augmentation=True)`
  forces per-champion variants. Default OFF per user preference for
  methodology-driven alternatives over post-hoc forced-champion brackets.
  **Observation B** — "stochastic-tilt mode made mean rank worse (620 vs
  532)" was the `leveraged` mode aggregate (MeanRnk 620.2) vs `seed` (532.4)
  across 17 yrs in `mc_pool_backtest_equalized_results.txt:272,275` (paired
  t=−3.808, p=0.0015, wins 3/17). The producing modes (`leveraged`,
  `opt_seed`, `opt_blend`, `opt_torvik`, `hedge_tv`) were retired from
  `mc_pool_backtest.ALL_MODES` per MEMORY.md §2 D6/D7; the stochastic-tilt
  utility `build_leverage_tilted_round_probs` and Pareto-leverage
  constructor `build_optimized_brackets` have zero production callers.
  **Leveraged mode regression is dead code; Pareto 5–14 collapse is
  documented design.** `[closed 2026-04-15; see §3 row 49 + artifacts/diversity_collapse_2026-04-15.json;
  drift guard tests/test_diversity_collapse_lock.py]`
- **Mean rank vs P(top 5 %) tell different stories.** Pools pay out top
  finishers, not averages. P(top 5 %) 8.3 % vs 5.1 % (a 63 % lift) was real
  signal; mean rank 532 wasn't. This observation is now locked forward by
  the 2026-04-02 strategic pivot (MEMORY.md §1 Strategic pivot) —
  production target is pool EV + P(1st) + P(top 5 %), not mean rank.
  `[locked by pivot; MEMORY.md §1 Strategic pivot]`

---

## 2. Open Questions / Unresolved for 2027

Stable-ID table so the council summoner (§4) can detect duplicates and check
current gaps before spawning another session. IDs are permanent — do not
renumber. When an item closes, move it to §1 and leave a `[closed <date>]`
crumb here.

**Status key:** `open` = nothing done • `partial` = scoped or started •
`mostly closed` = shipped but needs confirmation • `blocked:<Ox>` = waiting
on another item.

### Blocking — do first (prerequisites for 2027 predictions)

| ID | Item | Gate (how we know it's done) | Raised in §3 | Status |
|---|------|------------------------------|---|---|
| **O1** | Collect all 31 brackets from the actual 2026 pool | Structured dataset of 31 complete brackets (all 63 picks each), saved to repo. | 25 | **`[closed 2026-04-13]`** — `pool_hist_results.json` has 30 brackets as of 2026-04-12T22:47Z; the 31st is the user's own (correctly excluded to avoid a self-referential opponent model). Pattern verified across 2023 (18/19), 2025 (32/33), 2026 (30/31). See §1 Pool strategy: *pool size N ≠ usable opponent count K*. |
| **O2** | Validate eight local four-factor features against Torvik | Per-season, per-feature r ≥ 0.99, no systematic residual bias by team type / conference / tempo. Prereq: confirm Torvik docs state whether values are raw or opponent-adjusted. | 21 | **`[closed 2026-04-13]`** — resolved via the **barthag precedent from §3 row 22**, not by relaxing the gate. Investigation revealed the council's recommended path was already implemented in production at 5 call sites: `TorVikFFLookup.overlay_metrics` overlays Torvik's trank.php FF values onto local `ProprietaryTeamMetrics`, with local box-score computation as a fallback. The r ≥ 0.99 gate was measuring the wrong artifact (pre-overlay fallback values). Correct gates now locked in `tests/test_validate_four_factors.py`: (1) monthly Torvik FF snapshots present for every production year; (2) pre-tournament snapshot covers all 64-68 tournament teams (currently 68/68 for 2024); (3) local fallback tripwire `mean r ≥ 0.85` to catch catastrophic regressions like the O2a resolver collision. MEMORY.md §1 Validation/training split locks the Torvik-overlay pattern. Prereq "raw vs adjusted" was confirmed by code inspection + empirical match of Oliver formulas. |
| **O2a** | Resolver collision + data-source precision limit in the four-factor validator | Root cause #1 (fuzzy-containment `TeamNameResolver` collapsing `vermont_state_lyndon_hornets` → `vermont` and overwriting real Vermont Catamounts stats) + root cause #2 (provider-level precision limit making r ≥ 0.99 unreachable). | surfaced by O2 fresh validation 2026-04-13; both root causes addressed same day | **`[closed 2026-04-13]`** — root cause #1 fixed in `scripts/validate_four_factors.py` (commit `df8edce`), jumped 4/8 features from mean r ≈ 0.45 to ≈ 0.97. Root cause #2 resolved by applying the council's barthag precedent: Torvik is the production FF source via `TorVikFFLookup.overlay_metrics`; local is a fallback with a mean-r ≥ 0.85 tripwire for catastrophic-bug detection. See `tests/test_validate_four_factors.py` and MEMORY.md §1. |
| **O3** | Rank-correlation diagnostic on base model | Spearman ρ between predicted-P(1st) rank and actual historical pool placement, top ~10 brackets. If ρ ≈ 0, 2026 #11-ranking failure cannot be attributed between opponent model and base model. | 23, 24 | **`[closed 2026-04-13]`** — executed in `artifacts/rank_correlation_diagnostic.json`. Mean Spearman ρ = +0.37 across 14 years, 12/14 positive, median ρ = +0.46. Optimizer's P(1st) ranking has real directional signal. 2023 = −0.64 is the outlier reversal. |
| **O21** | Replace ESPN-national marginals with pool-specific marginals in opponent model | Opponent model rebuilt weighting ESPN-national + pool-history marginals; retrospective pool EV backtested against 2023-2026 to verify switch changes bracket rankings. | surfaced by O4 analysis 2026-04-13 | **`[closed 2026-04-13; partially reopened 2026-04-17 — see O26-G2a]`** — Shape-encoded verdict (2026-04-13): **null.** Spearman ρ between predicted P(1st) and actual score was flat at w=0/0.25/0.5 and slightly decreased at w≥0.75; concluded "marginals don't change rankings." **O26-G2a amendment (2026-04-17, team-identity rerun):** null is not encoding-invariant. Under team-identity scoring (consistent for pool brackets AND synthetic opponents) across 2023-2025 at K-1 per-year pool size, mean ρ by blend weight = **+0.709 / +0.767 / +0.627 / +0.718 / +0.712** at w=0.00/0.25/0.50/0.75/1.00 — all substantially higher than the shape baseline (+0.42-0.46 flat) and with a visible optimum at **w=0.25** (+0.767 vs +0.709 at ESPN-only). Per-weight Δρ vs shape: +0.25 to +0.31 across all blends, confirming the O26-G1 pattern (team-identity recovers signal that shape masks). The argmax best-weight flips from w=0.00 (shape) to w=0.25 (team-identity), but the magnitude of the best-weight advantage over ESPN-only is modest (+0.058 ρ on n=3 yrs, K=18/25/32) — not production-actionable on current evidence but not a null. Keep production locked 60/30/10 ESPN/Massey/seed pending larger-sample validation (flagged as a future candidate-generation upgrade per §3 row 58). See `artifacts/o26_g2_o21_team_identity_2026-04-17.json`, `scripts/o26_g2_o21_team_identity.py`, `ANALYSIS_O21_MARGINAL_BLEND.md` (shape-era context retained). |
| **O22** | Fix malformed `data/raw/historical/tournament_results_2026.json` | 67 games with correct `round_name` taxonomy: 32×R64, 16×R32, 8×S16, 4×E8, 2×F4, 1×NCG, 4×FF. Current file has 49×NCG and 0×R64, blocking any year-over-year analysis using 2026. | surfaced by O21 analysis 2026-04-13 | **`[closed 2026-04-15]`** — deterministic relabel, not re-scrape. Root cause: `src/data/scrapers/tournament_results.py::_parse_espn_event` uses a fragile substring matcher over ESPN event-note headlines; the `"championship"` pattern in `_ESPN_ROUND_MAP` collapses every round whose 2026-era headline contains the word "championship" but not a more specific round phrase onto `NCG`. The underlying game data (teams/scores/winners) in the file is intact — verified via a team-continuity audit: FF winners ⊆ R64 teams; R64→R32, R32→S16, S16→E8, E8→F4, F4→NCG are exact bijections. Repair (`scripts/repair_tournament_results_2026_o22.py`): idx 0..3 `R68`→`FF`; idx 4..35 `NCG`→`R64`; idx 36..51 `NCG`→`R32`; F4+NCG `region=""`→`"National"`. Post-repair counts match canonical {FF:4, R64:32, R32:16, S16:8, E8:4, F4:2, NCG:1}. Drift guard: `tests/data_integrity/test_tournament_results_taxonomy.py` (44 tests across every year 2005-2026, era-aware canonical counts — pre-2011 expects 1×FF, 2011+ expects 4×FF) fails loudly if any year ever drifts off the canonical label set. The scraper's substring matcher is a separate defect class (silent fallback to `R64` default when no pattern matches) — flagged for follow-up but outside this closure's scope. |

### High-priority validation gaps

| ID | Item | Gate | Raised in §3 | Status |
|---|------|------|---|---|
| **O4** | Empirical opponent correlation from the 30 real brackets | Measured correlation matrix across games; compared against independence assumption. | 25 | **`[closed 2026-04-13; locked 2026-04-14]`** — independence assumption holds. 4-year pooled Stouffer z = **−4.15** (per-year: 2023 z=−2.38, 2024 z=−2.18, 2025 z=−1.82, 2026 z=−1.91; all non-positive, 93 brackets total from `pool_hist_results.json`). Real pool brackets are *less* correlated than IID draws from the empirical marginals, not more. The opponent-model error lives in the *marginals* (5pp mean abs divergence from ESPN-national), not the correlation structure — see O21 closure for the marginal-blend follow-up (null result; locked 60/30/10 ESPN/Massey/seed). Narrative: `ANALYSIS_O4_OPPONENT_CORRELATION.md`. 2026-04-14 lock: re-serialized per-year stats to `artifacts/o4_opponent_independence_2026-04-14.json` via `scripts/o4_independence_lock.py` (reproducible under `rng_seed=42`, `n_sim_per_year=2000`); drift guard `tests/test_opponent_independence_lock.py` (7 tests) asserts pooled z stays < 0 (and < −1.0 with margin), no single year flips above +1.96, Stouffer formula consistency, and aggregate tolerance. |
| **O5** | MC sim count for stable rankings | Run optimizer 3× with identical inputs, verify top-20 rank-order identical. (Predicted jump 500 → 5000 sims.) | 24 | **`[closed 2026-04-13]`** — `n_tournaments=5000` locked in `MEMORY.md §1` Pool-strategy table; class/helper defaults normalized in `src/simulation/pool_competition.py:93,949` and `src/evaluation/evaluation_suite.py:371`; rank stability proven by `tests/test_pool_competition.py::TestRankStability::test_top20_rank_order_identical_across_runs` (3 runs at fixed seed produce identical top-20). |
| **O6** | Calibration check: simulated P(1st) vs actual placement | Historical verification that brackets ranked highest by optimizer actually won more often. | 23, 24 | **`[closed 2026-04-13]`** — signal is real and statistically significant; modest in magnitude and year-variable. Across 14 years (2011-2025 ex 2020): mean Spearman ρ = +0.37 between predicted P(1st) rank and actual pool placement (one-sided t-test p = 0.002; 12/14 years positive, binomial p = 0.006). Specifically on the winner-rank question: actual winner lands in top half of predicted ordering in **9/14 years** (vs 7 by chance); top third in **8/14** (vs 4.67 by chance); mean fractional rank of winner = **0.39** (vs 0.50 chance). The infamous §3 row 23 "1440-pt winning bracket ranked #11" case is the 2024 outlier (winner was 14th of 16 predicted). Council row 23's concern — whether the optimizer's P(1st) ranking has real signal at all — is empirically resolved YES. Evidence: `artifacts/o6_winner_rank_diagnostic.json`; gate locked by `tests/test_pool_optimizer_calibration.py` (7 tests: mean ρ > 0.20, t-test p < 0.05, ≥ 10/14 years positive, strict majority of winners in top half, mean fractional rank < 0.45). |
| **O7** | Tournament-only vs blended+stacked head-to-head | LOYO BSS comparison; tournament-only (500–600 games) vs blended (2200). Flagged 04-01; never run. | 3 | **`[closed 2026-04-13]`** — actually run on 2026-04-01 (`artifacts/baseline_experiment.json`); close analysis done today. Tournament-only `simple_7_logistic` LOYO paired-BSS vs seed baseline = **+4.8%** across 14 years (paired t-test p=0.036 uncorrected; fails Bonferroni across 4 configs at α=0.0125; Wilcoxon p=0.035; 11/14 year-wins). `fixed_27_logistic` tournament-only BSS = +1.0% (NS, p=0.75), confirming MEMORY §1 "7-feature logistic is the ceiling". Blended+stacked arm (LightGBM, XGBoost, SpreadRegressor, 27-feature + stacking) is MEMORY.md §2 D1: BSS = 0 across 17 yrs. Head-to-head: tournament-only simple_7 directionally edges blended+stacked (+4.8% vs 0%), but effect is marginal + Bonferroni-failing. §1 Validation lesson ("14-17-year backtests are underpowered ~9-16% power. Use them for binary/directional claims, not parameter tuning") means this does **NOT** re-open the 2026-04-02 pivot (§3 row 7) — the pool-EV argument was never about prediction accuracy. Evidence: `artifacts/o7_regime_comparison_2026-04-13.json`; gates locked in `tests/test_o7_regime_comparison.py` (6 tests). |
| **O8** | Feature collinearity with seed | Correlation matrix of 7 production features vs seed number. If any |r| > 0.7, BSS ≈ 0 is tautological. | 6 | **`[closed 2026-04-13]`** — **mixed result.** Diagnostic via `scripts/feature_seed_correlation.py::compute_production_feature_correlations()` over 612 team-year rows (2016–2025 ex 2020): max \|r\|=0.77 (`adj_off_eff`), median 0.32, with `diff_adj_tempo` (\|r\|=0.16) and `diff_opp_to_rate` (\|r\|=0.02) essentially independent of seed. Three features are seed proxies, two are independent — features carry signal beyond seed; BSS≈0 is **NOT** tautological (would need \|r\|≥0.85 across all features). Anti-tautology gate locked at `tests/test_feature_seed_collinearity.py::test_no_production_feature_is_total_seed_proxy` (fails if any feature breaches \|r\|=0.85). MEMORY.md §2 D1 updated with this evidence. |
| **O9** | Round-specific decomposition of "seeds beat everything" | Per-round BSS breakdown (R64 → NCG). Does the ceiling hold uniformly or break in E8/F4? | 7 | **`[closed 2026-04-13]`** — ceiling is **non-uniform but still intact**. `scripts/backtest_by_round.py` (17 yrs, 1071 games) shows: upset rate by round varies from 5.9% (NCG) to **42.6% (E8)** — +18pp gap. Later-rounds aggregate 29.8% vs early-rounds 24.5% (+5.3pp). So seed dominance weakens in later rounds. HOWEVER, per-round BSS of SI-adjusted vs seed: R64 +0.008, R32 −0.003, S16 +0.018, **E8 −0.010**, F4 −0.002, NCG +0.033 (n=17, underpowered). Even in the highest-upset round (E8), seed-independent signals show negative BSS — SI is slightly worse than seed. Later-round SI-quintile upset-rate difference is +0.2pp (flat). The non-uniformity does not open an exploitable gap for prediction-accuracy improvement; the §3 row 7 pivot is consistent with round-level evidence. Evidence: `artifacts/o9_round_decomposition_2026-04-13.json`; gates in `tests/test_o9_round_decomposition.py` (7 tests: ceiling non-uniformity, E8 most upset-prone, \|BSS\| < 0.05 in powered rounds, flat SI-quintile signal in later rounds, structure + consistency). |

### Design questions still open

| ID | Item | Gate | Raised in §3 | Status |
|---|------|------|---|---|
| **O10** | Opponent correlation: empirical from 1 pool-year vs theoretical copula | Decision + implementation. 30 brackets measures structure but generalization across 13 backtest years is unclear. | 25 | **`[closed 2026-04-14; conditional-reactivate]`** — **not needed; superseded by O4 lock.** The design question ("use a theoretical copula to model cross-bracket dependence that empirical data might miss") was predicated on real pools clustering more than IID. The O4 closure measured 4 years × 93 brackets and found the opposite — pooled Stouffer z = **−4.15**; every year individually negative. Real pools are *less* correlated than IID draws from the marginals. With O4 now hard-locked by `tests/test_opponent_independence_lock.py`, any copula work would be *regularizing a non-problem*. **Reactivation contract:** only if the O4 lock test fires (`pooled_stouffer_z >= 0` or any single year's z ≥ +1.96), re-open O10 in tandem — the copula would be the modelling response to genuine clustering. Until that fires, "independence + measured marginals" is the committed opponent-model shape. New MEMORY.md §2 **D14** dead-end. |
| **O11** | Is BSS ≈ 0 fatal to game-theory optimization? | Philosophical position (council 25): no — seed baseline is not zero-information. Codebase reflects this assumption but has no explicit invariant/test asserting it. | 25 | **`[closed 2026-04-13]`** — invariant locked in `tests/test_seed_baseline_informativeness.py` (6 tests, ~90ms). Asserts three concrete properties of the seed-lookup table that make game-theory pool optimization viable despite BSS=0 in the ML path: (1) extreme seed gaps produce extreme probabilities (P(1 vs 16) ≥ 0.85; today 0.99); (2) near-equal seeds are near coin flip (P(8 vs 9) ∈ [0.45, 0.60]; today 0.515); (3) seed-probability spread across all matchups ≥ 0.30; (4) monotonic in seed gap on average; (5) seed-lookup Brier < 0.25 uniform-random with BSS > 10% vs random (today: 0.213 Brier, 14.8% BSS vs random); (6) `seed_matchup_probability(a, b) + seed_matchup_probability(b, a) == 1.0`. The pool optimizer consumes this 14.8% of seed-embedded information regardless of whether any ML model improves over seed. |
| **O12** | Scoring-function structure modeled explicitly | Pool's point schedule (e.g. 1/2/4/8/16/32) hard-coded into optimizer's objective, not just game outcomes. | 23 | **`[closed 2026-04-14]`** — **gate already satisfied, locked forward.** `PoolEnvironment.scoring_rules: Dict[str, int]` is one of four mandatory env params (`src/optimization/pool_optimizer.py:30-52`); `validate()` raises on empty. `PoolOptimizer` forwards `env.scoring_rules` to `LeverageCalculator` and `bracket_portfolio` (≥ 3 propagation sites at `pool_optimizer.py:255,294,307,430`). Per-round points enter the EV objective at `leverage.py:1618+` via `pts = scoring_system.get("R64", 0)` → `expected_points += ev` (≥ 6 round references in the EV path, one per R64/R32/S16/E8/F4/CHAMP). Three parametric adapters registered by `get_scoring_adapter()` at `leverage.py:618-644`: standard (ESPN 10/20/40/80/160/320, `late_rounds` priority), flat (1/2/3/4/5/6, `balanced`), upset_bonus (ESPN weights + seed-diff multiplier). `AssumptionsManifest` records `scoring_rules` so every recommendation ships with the schedule. Evidence: `artifacts/o12_scoring_structure_audit_2026-04-14.md`. Drift guard: `tests/test_pool_scoring_structure.py` (8 tests, ~0.1s) — 7-row regression matrix covers each way the parametric schedule could collapse back to outcome-only. |
| **O13** | Winner-take-all: maximize variance not E[P(1st)] | Kelly-optimal risk posture evaluated against argmax-P(1st). | 24 | **`[closed 2026-04-14]`** — **stochastic (variance-aware) construction wins.** Head-to-head from `mc_pool_backtest_n31_det_vs_stoch.txt` (13 yrs × N=31 pool × 50 brackets × 50 repeats): stochastic `*_first_tv` arm BestRank = **1.52**, argmax `det_*` arm BestRank = **9.92**. Per-year: **stochastic wins 10/13**, argmax 3/13. Argmax has higher aggregate P(1st) (0.060 vs 0.041) but that's bimodal — dominated by 2015/2019 spikes (P(1st)=0.16/0.54) with zeros in 7 other years. Kelly / log-utility framing (concave utility, finite downside) favors stochastic consistency over argmax catastrophe risk. Production already locks `champ_first_tv` (recommended) and `e8_first_tv` (aggressive alt) per MEMORY.md §1 Pool-strategy; the `opt_*` P(1st)-maximizing modes were deprecated 2026-04-12 per MEMORY.md §2 D6 ("BestRank 62-88 vs seed's 38; P(1st)≈0 in upset years"). Evidence: `artifacts/o13_kelly_vs_argmax_audit_2026-04-14.md` + `artifacts/o13_kelly_vs_argmax_2026-04-14.json`. Drift guard: `tests/test_kelly_vs_argmax_lock.py` (7 tests, ~0.1s) asserts the BestRank invariant, MEMORY recommendation stays stochastic, both mode families remain in `ALL_MODES` for reproducibility, deprecated `opt_*` stays removed, and per-year winner majority stays stochastic. New dead-end D12 added to MEMORY.md §2. |
| **O14** | Calibration-to-pool interaction | Measure whether slightly upset-over-estimating calibration improves pool rank vs true calibration. | 23 | **`[closed 2026-04-14]`** — **gate hypothesis falsified.** Temperature sweep T ∈ {0.5, 0.75, 1.0, 1.25, 1.5, 2.0, 3.0} × 13 yrs × 200 stochastic brackets at `scripts/o14_calibration_sweep.py`. Brier-optimal T = **1.0** (honest calibration, by definition). P(beats chalk) is **monotonically decreasing** in T: 0.236 at T=0.5 → 0.224 at T=1 → 0.194 at T=3.0. Pool-rank-optimal T is *sharper* (T=0.5), not flatter — the **opposite direction** of the gate's "upset-over-estimating helps" prediction. Calibration-to-pool interaction exists (T=1 isn't exactly best-score-optimal — best-score ties at T=0.5 and T=1.5 with 914 vs T=1's 909) but (a) magnitude is trivial (≤ 5 ESPN points, ≤ 2 pp pBeatChalk) and (b) the dominant pool-rank lever is stochastic sampling (O13 closed), not calibration temperature. Decision: **keep T=1 honest calibration**; do not introduce a separate pool-calibration temperature. New MEMORY.md §2 D13 dead-end for upset-over-estimation. Evidence: `artifacts/o14_calibration_sweep_audit_2026-04-14.md` + `artifacts/o14_calibration_sweep_2026-04-14.json`. Drift guard: `tests/test_calibration_pool_interaction_lock.py` (6 tests) asserts Brier-optimal stays at T=1, pBeatChalk stays monotone decreasing in T, the best-score gap stays < 15 pts, and aggregate numbers within recorded tolerance. |
| **O15** | Vegas-line calibration vs in-house temperature scaling | Drop-in comparison on BSS + pool backtest metrics. | 4 | **`[closed 2026-04-14; conditional-reactivate]`** — **blocked on data access; ROI marginal even if unblocked.** Empirical audit 2026-04-14: no historical Vegas lines exist anywhere in the repo (`data/raw/` verified: no `*vegas*/*odds*/*market*/*spread*/*moneyline*` files; `external_7OT_*.json` are ratings, not market lines; `torvik_*.json::barthag` is a rating, not a spread; `data/raw/csv/*.csv` tournament results have no spread columns). The scraper that once populated this was deleted per §2 D10 (FanDuel/DraftKings → TheOddsAPIScraper → both gone from the tree). Free re-acquisition paths are exhausted: TheOddsAPI free tier is live-only (historical behind paywall ~$30-250/mo with thin NCAAB tournament coverage); Covers.com / SportsbookReviewOnline archives are now bot-protected; Kaggle March Mania data never shipped spreads. The `market_validation.py` consumer infrastructure is still wired and ready to accept `Dict[matchup_key, vegas_spread]`, so the code side isn't the bottleneck. **ROI argument:** even with data, the likely BSS margin of Vegas over simple_7-logistic is ~0.5-2% (basketball-betting-literature consensus). Given §3 row 7 pivot (stop optimizing accuracy; optimize pool EV), O7 closure (simple_7 tournament-only BSS = +4.8%), and O14 closure (calibration-to-pool interaction trivial at <5 ESPN points / <2 pp pBeatChalk), a Vegas swap-in is a second-order effect relative to the pool-EV pivot. **Reactivation contract:** (1) if any historical Vegas dataset lands on disk (e.g., `data/raw/vegas_lines_{year}.json`), OR (2) if a future §2 item reopens where the accuracy ceiling is the binding pool-EV constraint (which would flip the §3 row 7 pivot), re-open O15. Until either fires, committed state is "in-house temperature scaling is the calibration method; Vegas is a theoretical upper bound we neither pay for nor rely on." New MEMORY.md §2 **D15** dead-end row. |
| **O16** | `TOURNAMENT_START_DATES` hardcoded-dict SPOF | Test fails loudly when a year is missing; owner assigned for yearly update. | 22 | **`[closed 2026-04-13]`** — root cause was actually the silent fallback at `src/data/scrapers/torvik.py:400` (returned `(None, None)` on missing year, causing trank.php to serve unfiltered post-tournament data). Now raises `ValueError` for historical misses. Coverage test at `tests/data_integrity/test_tournament_start_dates.py` enforces `TRAIN_YEARS ∪ diagnostic ∪ holdout ⊆ TOURNAMENT_START_DATES`; owner placeholder added at `src/pipeline/config.py:47`; two stale script duplicates deleted. Follow-up → O16b. |
| **O16b** | Silent skips at `snapshot_integrity.py:70-72` and `test_selection_snapshot.py:66` | Same defect class as O16 but in a check (not a filter): replace silent skip with raise. Separate from O16 because the fix doubles blast radius without improving the gate. | surfaced by O16 fix 2026-04-13 | **`[withdrawn 2026-04-13]`** — re-read on 2026-04-13 confirmed both flagged call sites are actually correct. `snapshot_integrity.py:65-79` already raises `ValueError` at line 76 when a year is absent from both `SELECTION_SUNDAY_DATES` and `TOURNAMENT_START_DATES`; the `if year in` at line 71 is a nested fast-path, not a silent skip. `test_selection_snapshot.py:64-74` iterates over `SELECTION_SUNDAY_DATES` and conditionally asserts consistency *only for years also in* `TOURNAMENT_START_DATES` — a scope filter, not a leakage skip. No defect. Item retracted. |
| **O17** | Researcher-DoF leakage audit | Documented trail of which years were observable when which hyperparameters were tuned. | 19 | **`[closed 2026-04-14]`** — Audited inventory of every Optuna search-space bound (LightGBM 8 params, XGBoost 9 params, Logistic 2 params), global tuning budget (`optuna_n_trials=15` OOS-FIX reduced from 50; `enable_stacking=False`; `temporal_cv_splits=5`), temporal-CV policy (expanding-window, chronological), and year-visibility table (train: 2008-2024 ex 2020; holdout: [2025]; calibration on 2025 defensible because baseline models train on regular-season games only so tournament 2025 is OOS from base-model training) committed at `artifacts/o17_researcher_dof_audit_2026-04-14.md`. Code-level runtime enforcement is the O20-closure composition (`YearSplitPolicy.assert_dev_artifact_years` invoked from `_enforce_dev_only_years` at `hyperparameter_tuning.py:51`). Drift guard = `tests/test_researcher_dof_audit.py` (6 tests): fails if `optuna_n_trials`, `enable_stacking`, `temporal_cv_splits`, or any search-space bound drifts from the audit. Residual risk: pre-2026-04-07 tuning history not reconstructible (big-bang import commit `4a6605c` collapsed git graph); documented as accepted residual. Also: multiple-test correction not applied across 150 per-fit Brier evaluations — mitigated by narrow search bounds, not eliminated. MEMORY.md §1 "Researcher-DoF (tuning provenance)" row locks forward. |
| **O18** | Approximate-Torvik systematic bias on mid-majors / tempo profiles | Per-subgroup residual analysis (not just aggregate r² ≈ 0.95). | 19 | **`[closed 2026-04-13]`** — empirical verdict: bias is **uniform**, not subgroup-concentrated. Across 17 production years × 11 strata (2 conf tiers + 4 seed bins + 4 tempo quartiles), the cross-stratum range of mean bias for every feature is < 0.005 (turnover_rate 0.004, opp_turnover_rate 0.0025, defensive_reb_rate 0.0042, offensive_reb_rate 0.003) — well inside a 0.01 tripwire. Power vs mid-major `turnover_rate` bias differs by 0.001; 1-seeds vs 16-seeds by 0.0003; fast vs slow tempo by 0.004. The bias is the uniform provider-level delta characterized in O2 closure, not a signal-dependent subgroup skew. Gate enforced by `tests/test_validate_four_factors.py::test_local_ff_bias_is_uniform_across_subgroups` (catches any future change that preferentially mis-identifies a subgroup — e.g., TeamNameResolver regression on mid-majors). |
| **O19** | Pre-2011 ESPN picks data — exclude, down-weight, or equivalent | Decision + implementation. Field expanded 64 → 68 in 2011; analytics-culture shift. | 12 | **`[closed 2026-04-14]`** — **decision: keep pre-2011 archives for single-year historical fidelity; exclude from aggregated calibration.** Empirical basis: pre-2011 champions are 3/3 one-seeds (mean seed 1.00) vs 14 post-2011 champions averaging 1.86 (with 7-, 4-, 3-seed winners); public over-picks seed-1 at S16 by +14.5 points, E8 +12.6, F4 +8.4, CHAMP +4.9 vs post-2011 — a genuine regime shift, not sampling noise. Status quo was already compliant (`SEED_PICK_RATES` calibrated 2015-2024; `scripts/mc_pool_backtest.py` `BACKTEST_YEARS` starts at 2011) but not explicit in code. Closure makes the boundary enforceable: `MIN_PICKS_CALIBRATION_YEAR = 2011` constant + `strict_post_2011: bool = False` kwarg on `load_historical_public_picks` (raises `ValueError` pointing at O19 if a pre-2011 year is passed under strict mode). Archived pre-2011 JSON files stay on disk (2008 pool sim needs 2008 picks). Evidence: `artifacts/o19_pre_2011_picks_audit_2026-04-14.md`. Drift guard: `tests/test_pre_2011_picks_gate.py` (8 tests, ~0.1s) asserts the constant, the raise behavior, archive presence, and that `SEED_PICK_RATES` + `mc_pool_backtest.py` still respect the boundary. |
| **O20** | Held-out-last-3-years enforcement mechanism | Code-level gate that refuses to fit on the held-out window. | 14 | **`[closed 2026-04-13]`** — gate exists as a three-layer composition (verified end-to-end at `tests/test_holdout_enforcement.py`): (1) `validate_locked_production_path()` at `src/pipeline/config.py:901` hard-fails configs whose `holdout_years` drift from `[2025]` or whose `dev_years` overlap with holdout; (2) `_filter_years(include_holdout=False)` at `src/pipeline/pipeline_runner.py:496` strips holdout entries from any year list passed to training callers; (3) `YearSplitPolicy` at `src/ml/evaluation/evaluation_integrity.py:75` is a frozen dataclass that validates no overlap at construction and exposes `assert_dev_only` / `assert_dev_artifact_years` raising `HoldoutContaminationError` — wired at the three ensemble fit entry points + every Optuna tuner call. Note: council §3 row 14 phrased the gate as "last-3-years", but `MEMORY.md §1` locks holdout to the single year [2025]; the gate enforces the locked semantic. Locked in `MEMORY.md §1` Validation/training split table. |
| **O24** | Does opponent simulation produce different bracket rankings than P(score ≥ dynamic threshold)? | Run Spearman ρ between P(rank=1) rankings and P(score ≥ 97th-percentile-of-simulated-winner-scores) rankings on historical sim outputs. If ρ ≥ 0.95 AND top-decile concordance ≥ 0.97 across 2023-2026, opponent simulation is replaceable. Dynamic threshold = 97th percentile of max opponent scores per tournament sim (not hardcoded 1300). | 52 | **`[closed 2026-04-16 — FAIL; encoding-invariant 2026-04-17 per O26-G2b]`** — **Opponent simulation is load-bearing under both shape and team-identity.** Shape-encoded (2026-04-16): ρ = 0.20 / 0.05 / 0.56 across 2023-2025; top-decile concordance 0.33 / 0.00 / 0.67. Team-identity rerun (`scripts/o26_g2_o24_team_identity.py` → `artifacts/o26_g2_o24_team_identity_2026-04-17.json`): ρ = **0.31 / 0.16 / 0.34**; top-decile concordance **0.00 / 0.33 / 0.00**. Every year fails both gates (ρ ≥ 0.95, concordance ≥ 0.97) under team-identity — in fact 2025's ρ drops from 0.56 to 0.34, pushing the FAIL harder, and 2023/2025 top-decile concordance collapses to 0 (exactly the opposite of "rankings converge"). **Root cause unchanged**: the 97th-percentile winner-score threshold (~1560 pts, up slightly from shape's ~1360 under team-identity) is unreachable for any individual bracket (p_threshold ≈ 0.000–0.003). Rankings by p_threshold collapse to near-uniform noise regardless of scoring. A bracket wins the pool by being the *highest scorer in that trial*, not by crossing a high absolute threshold — the joint structure (opponents face the same tournament realization) is what the simulation captures and the threshold discards. **Do not replace the opponent simulation with a score threshold.** pool_hist_results.json is not load-bearing for bracket *selection* (O21), but the opponent simulation structure itself is. Evidence: `artifacts/o24_threshold_ranking_2026-04-16.json` (shape); `artifacts/o26_g2_o24_team_identity_2026-04-17.json` (team-identity); `scripts/o24_threshold_ranking_test.py`, `scripts/o26_g2_o24_team_identity.py`. |
| **O25** | Does replacing P(rank=1) with the empirical supremum P(b_i > max(actual pool opponents)) improve within-portfolio bracket selection? | Three dependency-ordered gates: **G1 (spread diagnostic + 2026 post-mortem)** — for each of the 50 brackets in 2026 extract the P(rank=1) assigned by the current ranker and the actual rank outcome; compute the spread between #1 and #11; check whether #11 had a structurally distinct upset profile. Output: table of P(rank=1) vs actual rank for 2026. Gate: if spread < 1% → selection optimization is noise trading, pivot to G3 (portfolio diversity); if spread > 3% → systematic error, proceed to G2. **G2 (empirical supremum objective)** — using pool_hist_results.json, compute max(opponent scores) across 5000 MC runs using the *actual* 30-person pool bracket corpus; rank 50 candidates by P(b_i > empirical max); verify O4's anti-correlation (z=−4.15) is reflected by checking the new ranking favors higher-variance upset brackets relative to the current ranker. Gate: Spearman ρ between new ranker and actual 4-year outcomes > 0.50, OR 2026 winning bracket (actual rank #11) ranks in top 5 under new objective. **G3 (portfolio diversity fallback, if G1 narrow)** — measure pairwise score correlation across 50-bracket portfolio; add diversity penalty to generation targeting the tail of the score distribution. Gate: average pairwise correlation < 0.60 AND portfolio contains at least one bracket in top decile of upset-count distribution. **Forbidden paths** (explicit dead-end guards): conditional clustering by chalk/upset regime risks re-litigating D6/D16; feature classifier on binary "did this bracket win?" is statistically non-viable with 4 positive examples across 200 observations (O23-E rejection). **O4 effect propagation**: z=−4.15 (opponents less correlated than IID) means max(opponents) has *higher* variance than the IID case — systematically favors high-upside upset brackets in the supremum calculation; the current ranker modeling opponents as near-IID systematically underweights this. O21 null result means opponent *marginals* are not the lever; the *joint tournament realization* structure is. | 53 | **`[closed 2026-04-16 — dead-end D17]`** — **G1 REDONE (the prior 2026-04-16 G1 narrative in this row was undocumented and unreproducible — no script, no artifact on disk, numbers cited (spreads 0.835/0.315/0.045/0.310) appear nowhere else in the repo, and are inconsistent with the pre-existing `rank_correlation_diagnostic.json`).** Real G1 (`scripts/o25_g1_spread_diagnostic.py` → `artifacts/o25_g1_real_2026-04-16.json`): 3 ranker definitions × 4 years = 12 spread measurements, all in [0.016, 0.047]; **9 of 12 AMBIGUOUS** (1%-3% zone), 3 of 12 barely > 3%. Cross-reference with pre-existing `rank_correlation_diagnostic.json` (production portfolio, n=15-17): 2023=0.028 (ambiguous), 2024=0.060 (fixable), 2025=0.092 (fixable). Actual-best bracket of 50 placements: within-portfolio #19/#5/#2/#47, vs actual opp #16/#23/#33/#46, vs IID #36/#24/#30/#37. 2026 actual-best has 5 R64 upsets vs portfolio median=6 — it is NOT a structurally distinct upset profile, ruling out the "ranker underweights upsets" hypothesis in the §2 O25 spec. **G2 RUN UNDER BOTH SCORINGS (FAIL both):** (1) **Shape-encoded G2** (`artifacts/o25_g2_empirical_supremum_2026-04-16.json`): mean ρ(p_sup, actual) across 4 yrs = **+0.0605**, baseline ρ(p_rank1, actual) = **−0.0500**. G2-B: 0/4 flips. G2-C: 2/4 years pass. (2) **Team-identity G2** (`artifacts/o25_g2_team_identity_2026-04-16.json`, real ESPN scoring, the one that actually determines pool payouts): mean ρ(p_sup, actual) = **+0.0086**, baseline ρ(p_rank1, actual) = **+0.0660** — under team-identity scoring the OLD within-portfolio P(rank=1) is marginally BETTER than the proposed empirical supremum. G2-B: 0/4 flips. Per-year team-identity ρ_new: 2023=−0.12, 2024=+0.02, 2025=+0.29, 2026=−0.15. G2-A gate > 0.50 **fails by an order of magnitude under both encodings**. The empirical-supremum objective is not a measurable improvement over P(rank=1). **Coverage is not the bottleneck:** under team-identity scoring the 50-candidate portfolio contains a bracket that beats the stored pool winner pts in ALL 4 years (2023 1220>720, 2024 1400>1380, 2025 1660>1600, 2026 1520>1350). The problem is selection, not generation. **G3 PASS (unchanged from 2026-04-16 G3 commit):** portfolio is already near-zero correlated (mean pairwise r = 0.03-0.07). **Closure outputs:** `MEMORY.md §2 D17` (amended); `tests/test_o25_g1_g2_lock.py` locks both shape and team-identity aggregates; three scripts preserved (`o25_g1_spread_diagnostic.py`, `o25_g2_empirical_supremum.py`, `o25_g2_team_identity.py`). Conditional reactivate: a materially different candidate-generation strategy (tail-diversity rather than champ_first_tv) that produces ρ > 0.50 under team-identity scoring would re-open. **Do not re-attempt empirical-supremum bracket selection on champ_first_tv torvik portfolios under either encoding.** New MEMORY.md §2 **D17** (amended). |
| **O23** | Chalk-bias-aware bracket construction beating production `champ_first_tv` | Construction-layer mode that uses the empirical per-(round, seed) chalk-bias signal (anchor: seed-1 F4 = 2.00× over-pick per `src/data/seed_pick_model.py:300-403`) to outperform the production stochastic mode on a head-to-head pool backtest. **G1 — Empirical bias table.** Compute per-(round, seed) public_pct vs model_pct table from `data/raw/historical_public_picks/` (post-2011 per O19 `MIN_PICKS_CALIBRATION_YEAR`); emit `artifacts/chalk_bias_table_<date>.json`. Reuse `src/data/historical_picks.py::load_historical_public_picks` and the multiplier scaffold at `src/data/seed_pick_model.py:300-403`. **G2 — Construction-layer change.** Implement `champ_first_chalkfade_tv` (or equivalent) sampler that uses the G1 table to fade specifically over-picked (round, seed) pairs. **Forbidden paths** (each maps to a recorded failure mode that this gate explicitly prevents re-tripping): static cap tuned from historical average (§1 Pool-strategy "F4 cap at 2 not 1.5" small-sample warning); round-agnostic top-quintile fade (MEMORY.md §2 D8: 19.4% < 23.2% base rate, lost 7.1% to chalk over 853 matchups); marginal-shift-only adjustment that doesn't change recommended-bracket *ranking* (§2 O21 null result on opponent-model marginal blending). **G3 — Head-to-head backtest** in `scripts/mc_pool_backtest.py` ALL_MODES vs `champ_first_tv` over 13 yrs (post-2011), N=31, 50 stochastic brackets × 50 opponent repeats — matches MEMORY.md §3 Pool-backtest scope. **G4 — Significance gate, paired across years, Bonferroni across 3 metrics (α = 0.05/3 = 0.0167):** BestRank chalkfade ≤ champ_first_tv (paired t p < 0.0167) AND P(1st) chalkfade ≥ champ_first_tv (paired t p < 0.0167) AND P(top 5%) chalkfade ≥ champ_first_tv (paired t p < 0.0167). **G5 — Ranking-stability cross-check** (O21-style): mean Spearman ρ between predicted P(1st) and actual pool placement must NOT degrade vs `champ_first_tv` (reuse `artifacts/rank_correlation_diagnostic.json` methodology). Catches the failure mode where chalkfade improves portfolio metrics but worsens within-portfolio ranking. **Closure rule:** all 5 gates pass → close, lock new mode in MEMORY.md §1 Pool-strategy, add lock test (drift guard for backtest numbers). Any gate fails → archive evidence to `artifacts/`, add MEMORY.md §2 **D16** dead-end row with the failing-metric rationale, close as dead-end. **Do NOT ship without clearing all 5 gates.** | 7, 17 (and §1 Pool-strategy chalk-bias bullet) | **`[closed 2026-04-15 — dead-end D16]`** — **G1 passed** (artifact built; seed-1/F4 ratio = 1.26, adjusted gate [1.0, 2.5] to correct for per-team vs dominant-team averaging). **G2 passed** (`champ_first_chalkfade` mode implemented end-to-end through CLI + backtest). **G3 FAIL**: 13-yr N=1000 backtest (`mc_pool_backtest_20260415_040656.txt`) shows `champ_first_chalkfade_tv` P(1st) = **0.04%** vs `champ_first_tv` **0.56%** (14× worse); BestRnk 19.5 vs 18.8 (worse). MeanRnk improved (490.9 vs 505.2) but is the wrong metric for winner-take-all. **G4 N/A** — G3 failed; paired t-test confirms p ≈ 0.49 (non-significant in either direction). **Root cause**: dividing champion draw weights by the empirical chalk ratio (seed-1/CHAMP = 1.22×) modestly depresses the probability of selecting the actual tournament champion. The fade trades tail upside (P(1st)) for marginally better average position (MeanRnk) — the exact inverse of what winner-take-all pool optimization requires. **Secondary finding**: the static `_chalk_multiplier` anchor (2.00×) overestimated seed-1/F4 chalk by ~60%; true per-team empirical average is 1.26×. The G1 artifact and `load_chalk_bias_table()` are correct and remain usable by future experiments. **Do not re-attempt champion-weight fade without a mechanism to improve P(1st) simultaneously.** Evidence: `artifacts/chalk_bias_table_2026-04-15.json`, `artifacts/backtest_runs/mc_pool_backtest_20260415_040656.txt`. New MEMORY.md §2 **D16**. |
| **O26** | Scoring-function divergence: `score_brackets_against_outcome` (shape encoding) vs real ESPN team-identity scoring. Shape encoding credits positional bool match across 63 games; team-identity credits picked-team membership in per-round winner sets. Every pipeline measurement that scores brackets against ACTUAL outcomes uses shape encoding, but the pool actually pays out under team-identity. **Spot-check (2026-04-16, `scripts/o26_rank_corr_team_identity_spotcheck.py`):** over 2023-2026 (10-19 brackets/year via same portfolio gen as `rank_correlation_diagnostic.py`), mean ρ_shape = **+0.049**, mean ρ_team_identity = **+0.491** — a **10× magnitude gap and sign inversion** in upset-heavy years (2023: ρ_shape = −0.25, ρ_team = **+0.93**; 2024: ρ_shape = −0.28, ρ_team = **+0.78**). mean \|Δρ\| = 0.68. The two encodings don't approximate each other. **Gates:** **G1 (14-yr rerun of rank_correlation_diagnostic under team-identity)** — reproduce the O6 baseline (MEMORY.md §1 "ρ=+0.37") with team-identity scoring across 2011-2025 ex 2020. Gate: if mean ρ_team differs from ρ_shape by > 0.15, amend MEMORY.md §1 with the corrected number; otherwise close as bookkeeping. **G2 (audit `rank_correlation_diagnostic.json` claims that shifted)** — per-year, decide which closure (O6 §1 row, O21 null, O24 FAIL, D12/O13) needs amendment based on team-identity re-run. **G3 (production MC backtest re-run, `scripts/mc_pool_backtest.py`)** — re-run the 13-yr production backtest under team-identity scoring. Gate: if production-mode rankings (champ_first_tv vs alternatives) shift, update MEMORY.md §3 Pool backtest table + POOL_STRATEGY_RECOMMENDATION.md. **G4 (migrate or flag `score_brackets_against_outcome`)** — add a team-identity companion function in `src/simulation/pool_competition.py`, migrate production call sites (leverage/EV is already team-identity-aligned; MC-based path is the remaining gap). Test drift guards updated to use team-identity where it changes the claim. **Forbidden paths:** do NOT delete the shape-encoded function (it's fast and usable for within-MC consistency checks — just not for actual-pool comparisons); do NOT relax the existing O6 / O24 / D12 test tolerances without a team-identity re-run as evidence. **Conditional reactivate rules:** if any production closure materially flips under team-identity, that closure row must be re-opened before downstream work continues. Priority: G1 first (cheapest check on the headline O6 number), then G2 scope, then G3 (biggest wall-clock). | Spotcheck: `artifacts/o26_rank_corr_team_identity_spotcheck_2026-04-16.json`; §1 O6 row (mean ρ=+0.37); §3 Pool backtest table; §2 O21/O24/D12 | **`G1+G2+G3-narrow+G4-companion complete 2026-04-17; O27 (N=1000 + noseed/blend) and G4 MC-call-site migration open`** — **G1 RESULT (`scripts/o26_g1_full_14yr_rerun.py` → `artifacts/o26_g1_full_14yr_rerun_2026-04-17.json`):** 15 years (2011-2026 ex 2020), N_PLACEMENT_TRIALS=200 matching O6 baseline settings exactly. Shape companion reproduces the O6 baseline: mean ρ_shape = **+0.361** (median +0.422, 87% positive, t=+3.55 p=0.003) — within 0.01 of O6's recorded +0.37, confirming the original methodology was shape-encoded as suspected. Team-identity: mean ρ_team = **+0.613** (median +0.688, **100% positive across all 15 years**, t=+8.53 p<1e-6). **\|mean Δρ\| = 0.252**, exceeding the pre-committed 0.15 amend threshold by 70%. Per-year ρ_team: positive in every year (15/15); three years show ≥+0.5 Δ over shape (2011 +0.90, 2023 +1.39, 2025 +0.54). **MEMORY.md §1 O6 row amended 2026-04-17** with the +0.61 team-identity number. **G2 RESULTS (three per-closure reruns, 2026-04-17):** (a) **O21 partially reopens** — team-identity per-blend-weight mean ρ (2023-2025, K-1 per-year pool) = +0.709/+0.767/+0.627/+0.718/+0.712 at w=0/0.25/0.5/0.75/1.0 vs shape +0.42-0.46 flat; argmax-best weight flips from 0.00 (shape) to 0.25 (team-identity); Δρ vs shape +0.17 to +0.31 across weights. Signal exists but the best-weight advantage over ESPN-only is modest (+0.058 ρ on n=3 yrs) — not production-actionable yet, flagged for larger-sample follow-up (`scripts/o26_g2_o21_team_identity.py` → `artifacts/o26_g2_o21_team_identity_2026-04-17.json`). (b) **O24 encoding-invariant FAIL** — team-identity ρ = 0.31/0.16/0.34 across 2023-2025; top-decile concordance = 0.00/0.33/0.00; FAIL harder under team-identity than shape (2025 ρ drops 0.56 → 0.34). Opponent simulation is load-bearing regardless of encoding (`scripts/o26_g2_o24_team_identity.py` → `artifacts/o26_g2_o24_team_identity_2026-04-17.json`). (c) **D12 encoding-invariant, strengthened** — narrow-scope stoch-vs-argmax (champ_first_tv vs det_champ_tv) at N=31, 14 yrs (2011-2025 ex 2020): stochastic BestRank **1.79** vs argmax **7.44**; **stochastic wins 14/14 years under team-identity** (shape baseline: 10/13). Argmax P(1st) still marginally higher on aggregate (0.035 vs 0.034) but that's bimodal — 2019 carries it at 0.39 vs stoch 0.08, consistent with D12's "chalky-year bimodality" narrative. Keep D12 locked (`scripts/o26_g2_d12_team_identity.py` → `artifacts/o26_g2_d12_team_identity_2026-04-17.json`). **G3 RESULT (narrow, N=31, 9 of 11 modes; noseed+blend deferred):** `scripts/o26_g3_mc_backtest_n31_team_identity.py` → `artifacts/o26_g3_n31_team_identity_2026-04-17.json`. 14 yrs (2011-2026 ex 2020, 2012 skips on missing ESPN picks), 50 brackets × 50 repeats, ESPN opponents. **Material BestRank reordering:** shape top-5 order f4_first_tv (1.50) / e8_first_tv (1.50) / champ_first_tv (1.60) / seed (1.60) / torvik (2.00) vs team-identity torvik (**1.42**) / e8_first_tv (1.87) / f4_first_tv (2.06) / seed (2.30) / champ_first_tv (**2.67**). Argmax BestRank flipped (f4 → torvik); top-3 order rearranged; **production-recommended `champ_first_tv` drops to worst BestRank of the 5 headline modes** under team-identity. **P(1st) argmax preserved** — torvik wins both encodings (shape 0.0445 vs team-identity 0.0417), so the WTA winner-take-all verdict is not invalidated. `det_*` modes show extreme bimodality under team-identity (e.g., `det_e8_tv` 2026 P(1st)=0.400 carries aggregate 0.0717) — consistent with D12 caution. Per pre-committed gate, N=31 reordering triggers escalation; **N=1000 rerun deferred** per user decision (2026-04-17) as **O27** below. MEMORY.md §3 Pool backtest table carries a team-identity caveat note pending O27 resolution; do not cite shape-encoded BestRank ordering as authoritative for new mode recommendations until O27 closes. **G4 companion landed:** `src/simulation/pool_competition.py` now exports `score_brackets_team_identity`, `picks_by_round`, and `actual_winners_by_round` as public peers to the shape scorer; the shape scorer's docstring now carries an explicit encoding warning + pointer to the companion (closes the class of mixed-encoding bug that G2-a surfaced in the original O21 script). Call-site migration of `scripts/mc_pool_backtest.py` (the MC-based path) is **explicitly gated behind §2 O27** — touching it before the N=1000 rerun would leave MEMORY §3 stale-shape-encoded while code produced team-identity. See `tests/test_pool_competition.py::TestTeamIdentityScoring` (8 new unit tests, including hand-computed shape-vs-team-identity divergence on an upset case) + `tests/test_o26_g2_lock.py` G4 acceptance guards (API presence, docstring anchor, signature stability, NCG→CHAMP contract). |
| **O27** | G3 escalation — rerun production MC backtest at N=1000 under team-identity to confirm / refute G3-narrow's BestRank reordering. | Run `scripts/mc_pool_backtest.py`-equivalent at 14 yrs × all 11 modes (incl. noseed + blend; requires lightgbm/xgboost/torch) × N=1000 × 50×50 with team-identity scoring. Emit artifact + aggregate table directly comparable to MEMORY §3. Gate: if team-identity N=1000 preserves the G3-narrow reordering (argmax BestRank flips OR top-3 rearranges), amend MEMORY §3 Pool backtest table + POOL_STRATEGY_RECOMMENDATION.md with the team-identity numbers and re-evaluate the `champ_first_tv` recommendation. If N=1000 reverses the reordering (shape and team agree at production pool size), G3-narrow becomes a small-pool-only caveat — amend POOL_STRATEGY's N=31 sub-section and close O27. **Deferred follow-up:** P(1st) argmax is already preserved at N=31 (torvik wins both encodings), so the WTA recommendation path is less urgent to re-verify than the BestRank ordering. Compute budget: ~2-4 hrs including ML-stack install. Conditional reactivate: any new 2026 pool performance claim that relies on BestRank ordering triggers this before the claim can land. | surfaced by O26-G3-narrow 2026-04-17 | **`open`** — N=1000 rerun not yet started. Evidence to date: G3-narrow at N=31 shows the reordering; P(1st) argmax preserved. |

---

## 3. Session Index (chronological)

Terse index so you can grep for "what did the council say about X".
Format: `date — question → verdict`. Follow-up or critical items linked by
number to §2.

| # | Date | Question | Verdict |
|---|------|----------|---------|
| 1 | 2026-03-31 | Biggest repo limitation? | Stacking weight contamination; no revoke-and-relock protocol. *(Moot post-pivot.)* |
| 2 | 2026-04-01 05:25 | Most critical limitation? | Training-data starvation; no baseline comparison against seed-only. |
| 3 | 2026-04-01 05:58 | Train on reg-season + tournament? | No — distribution mismatch, not sample-size fix. Don't add NIT. Tournament-only diagnostic flagged *(open → §2 O7)*. |
| 4 | 2026-04-01 21:21 | Single most critical limitation? | Calibration on 60–70 games is fatal; use Vegas lines. 7-feature logistic is the ceiling. |
| 5 | 2026-04-02 05:20 | First priority after backtest harness? | Wire seed-only baseline into LOYO eval loop. If pipeline doesn't beat seed @ p<0.05, most of 122 K LOC is noise. |
| 6 | 2026-04-02 20:52 | Biggest limitations? | BSS ≈ 0 vs seed baseline. Feature collinearity with seed possibly tautological *(open → §2 O8)*. |
| 7 | **2026-04-02 22:03** | Where to focus next? | **STRATEGIC PIVOT: stop optimizing accuracy; optimize pool EV.** 5-advisor unanimous. *(MEMORY.md §1)* |
| 8 | 2026-04-03 v2 | Next step for pool optimizer? | Build MC simulation to measure P(rank=1). Brier ≠ pool performance. |
| 9 | 2026-04-03 v3 | Skip per-round Brier gate check? | Yes, unanimous — Brier doesn't proxy P(rank=1). Build MC directly. |
| 10 | 2026-04-03 v4 | MC results: noseed = seed at P(1st) ≈ 0.054. What now? | Fix backtest (argmax → stochastic), then validate opponent model. |
| 11 | 2026-04-03 v5 | Stochastic backtest near-random. Why? | Opponent model broken (stale SEED_PICK_RATES proxy). Swap in real ESPN aggregate picks. |
| 12 | 2026-04-04 | 19-yr ESPN data ready? | No — team-name matching bug (silent fallback). Build diagnostic, ≥ 80 % match rate gate before backtest. Pre-2011 ≠ post-2011 *(open → §2 O19)*. |
| 13 | 2026-04-04 v2 | Can we run backtests now? | No, unanimous — fix team-name matching first. Literal lookup table for ~120 tournament teams. Silent fallback = flying blind. |
| 14 | 2026-04-04 v3 | Post-fix: next step? | Re-run stochastic backtest; pre-commit kill criterion (e.g. median rank < 400 / 1000). |
| 15 | 2026-04-04 v4 | Why are backtests random? | Optimizer bypassed in backtest harness; inject chalk-bias signal directly if leverage ≈ 0. |
| 16 | 2026-04-04 v5 | Is opt_mode `mean_rank=396` real? | No — selection bias (5 optimized vs 50 seed brackets). Fix parity. |
| 17 | 2026-04-04 v6 | Path A/B/C? | Path C, but diagnose leveraged-mode regression first *(open — see §1 Diversity)*. P(top 5%) is the real signal, not mean rank. |
| 18 | 2026-04-04 v7 | opt_torvik p=0.027 significant? | No after Bonferroni (6 modes, threshold p<0.0083). Confirm Torvik provenance before anything else. |
| 19 | 2026-04-04 v8 | Leakage / provenance audit? | Add `_validate_pretournament()` runtime guard. Don't trust aggregate barthag r² ≈ 0.95 — check per-subgroup *(open → §2 O18)*. Researcher-DoF leakage flagged *(open → §2 O17)*. |
| 20 | 2026-04-05 v9 | Most critical unresolved gap? | Runtime guard + 10 regression tests landed. Guard must live at data-loading layer, not caller-dependent. |
| 21 | 2026-04-06 v10 | Local four factors valid for backtest? | No — validate against Torvik, gate r ≥ 0.99 *(open → §2 O2)*. Barthag r=0.73 is the precedent. |
| 22 | 2026-04-06 03:07 | trank.php vs cbbdata.com as primary? | **Promote trank.php** for server-side date filtering (redundant leakage guard). `TOURNAMENT_START_DATES` is SPOF *(open → §2 O16)*. |
| 23 | 2026-04-12 | Critical steps for 2027? | Optimize P(1st) not EV; enforce F4 historical base rates; validate opponent model. 2026 system ranked its own winning bracket #11. |
| 24 | 2026-04-12 b | Next critical steps? | Fix opponent correlation (1), bump MC sims 500 → 5000 (5). Do NOT tune F4 cap 2 → 1.5 (overfitting). |
| 25 | **2026-04-12 c** | Single most critical action for 2027? | **Collect all 31 brackets from actual 2026 pool this week.** Prerequisite to every opponent-model fix. Independence assumption is a fundamental validity threat. *(MEMORY.md §3 governing; §2 O1)* |
| 26 | 2026-04-13 | Close O16: TOURNAMENT_START_DATES SPOF? | **Closed.** Real defect was the silent fallback at `torvik.py:400` (returned `(None, None)` and let trank.php serve unfiltered post-tournament data). Now raises `ValueError`. Coverage test enforces `TRAIN_YEARS ⊆ dict`. Two stale script duplicates removed. Spawned **O16b** for the same defect class in `snapshot_integrity.py`. *(§2 O16 closed; §2 O16b open)* |
| 27 | 2026-04-13 | Close O5: bump MC sims for stable rankings? | **Closed.** `n_tournaments=5000` locked in `MEMORY.md §1` Pool-strategy table. Class/helper/eval-suite defaults normalized (were drifting at 1000 even though the three CLI call sites had bumped to 5000 on 2026-04-12). Rank-stability test (`tests/test_pool_competition.py::TestRankStability`) proves 3 runs at fixed seed produce identical top-20 over 20 brackets. *(§2 O5 closed)* |
| 28 | 2026-04-13 | Close O8: are production features just seed proxies? | **Closed — mixed result, BSS≈0 NOT tautological.** `scripts/feature_seed_correlation.py::compute_production_feature_correlations()` over 612 team-year rows: max \|r\|=0.77 (`adj_off_eff`), median 0.32; `diff_adj_tempo` (\|r\|=0.16) and `diff_opp_to_rate` (\|r\|=0.02) carry information independent of seed. Anti-tautology gate (`tests/test_feature_seed_collinearity.py`) fails if any feature ever crosses \|r\|=0.85; MEMORY.md §2 D1 updated with the evidence. *(§2 O8 closed)* |
| 29 | 2026-04-13 | Close O20: code-level holdout-window enforcement? | **Closed.** Gate is a three-layer composition: (1) `validate_locked_production_path()` config gate, (2) `_filter_years(include_holdout=False)` data filter, (3) frozen `YearSplitPolicy` raising `HoldoutContaminationError`. Wired at all three ensemble fit entry points + every Optuna tuner call. End-to-end closure test at `tests/test_holdout_enforcement.py`; locked in `MEMORY.md §1` Validation/training split. The "last-3-years" phrasing from council row 14 is honored as MEMORY's locked single-year holdout `[2025]`. *(§2 O20 closed)* |
| 30 | 2026-04-13 | Withdraw spawned O16b? | **Withdrawn — analysis error.** Re-read of `src/evaluation/snapshot_integrity.py:65-79` and `tests/evaluation/test_selection_snapshot.py:64-74` confirmed both call sites are correct. The function raises `ValueError` at line 76 for missing years; the test's `if year in TOURNAMENT_START_DATES` is a scope filter for a dual-membership consistency check, not a silent leakage skip. No defect. *(§2 O16b withdrawn)* |
| 31 | 2026-04-13 | Close O2: Four-Factor validation? | **NOT CLOSED — escalated to a council decision.** Two independent defects found: **#1 (resolver collision, FIXED):** `compute_boxscore_ff` was letting TeamNameResolver's fuzzy `containment` method collapse `vermont_state_lyndon_hornets` (1 game) onto canonical `vermont`, overwriting real Vermont Catamounts stats. Fix applied. **#2 (data-source precision limit, ESCALATED):** audit across 344 D1 teams in 2024 shows that even with the best-possible formula, local-vs-Torvik Pearson saturates at r ≈ 0.97 — including for eFG% which has no possessions denominator. This is a scorekeeping-level provider difference, not a code defect. Three council options offered in `artifacts/o2a_diagnostic_2026-04-13.md`: (A) relax gate to r ≥ 0.95, (B) switch to a second box-score provider, (C) accept "r ≈ 0.97" as validated. Recommendation: A. *(§2 O2 blocked on council; §2 O2a root-cause-1 closed, root-cause-2 escalated to O2)* |
| 32 | 2026-04-13 | Close O2 via council-precedent review? | **CLOSED — council's own §3 row 22 (barthag) precedent applied.** Re-reading §1 lines 96–98 ("Validate before shipping. Formula-correctness is not validation. The barthag r = 0.73 divergence is the precedent to remember") + §1 line 112 ("Define kill-criteria BEFORE running tests. Without a pre-committed threshold you rationalize whatever number comes back") rejected row-31's Option A (relax gate → rationalize). Instead applied Option D implied by §3 row 22: **promote Torvik as the production FF source**, demote local to fallback. Discovery: this was **already implemented** in production at 5 call sites via `TorVikFFLookup.overlay_metrics`. The r ≥ 0.99 gate had been pointed at the wrong artifact (pre-overlay fallback values that production never uses as-is for covered teams). Correct gates locked in `tests/test_validate_four_factors.py` (19 tests passing): monthly-snapshot coverage per year, tournament-team coverage in pre-tournament snapshot, local-fallback mean-r ≥ 0.85 tripwire. MEMORY.md §1 Validation/training split updated with the locked pattern. *(§2 O2 closed; §2 O2a closed)* |
| 33 | 2026-04-13 | Close O18 on the back of the O2 close? | **CLOSED — bias is uniform, not subgroup-concentrated.** `scripts/validate_four_factors.py` already emits stratified residuals by conference tier (power / mid-major), seed bin (1-4 / 5-8 / 9-12 / 13-16), and tempo quartile (slow / med_slow / med_fast / fast). Aggregating `artifacts/o2_ff_validation_2026-04-13.json` across 17 production years × 11 strata shows cross-stratum bias range < 0.005 for every feature. Power vs mid-major `turnover_rate` bias differs by 0.001 — three orders of magnitude smaller than the O8 seed-correlation signal, confirming the provider-delta hypothesis from O2 (it's scorekeeping-level, not signal-dependent). Gate locked at cross-stratum range ≤ 0.01 in `tests/test_validate_four_factors.py::test_local_ff_bias_is_uniform_across_subgroups` (1 test, ~90s). *(§2 O18 closed)* |
| 34 | 2026-04-13 | Close O6: is P(1st) ranking calibrated vs actual? | **CLOSED — signal is real and statistically significant; modest in magnitude and year-variable.** Re-analyzed O3's `artifacts/rank_correlation_diagnostic.json` (14 backtest years × 15–25 generated brackets per year) for the specific winner-rank question O6 posed. Aggregate: mean ρ = +0.37 (one-sided t p=0.002), 12/14 years positive (binomial p=0.006). On winner-specific calibration: actual winner lands in top half of predicted ordering in 9/14 years (vs 7 by chance), top third in 8/14 (vs 4.67), mean fractional rank = 0.39. The 2024 "#11 in own portfolio" failure from council row 23 is the tail outlier (winner was 14th of 16 predicted), but the population-level signal is directionally correct. Evidence committed as `artifacts/o6_winner_rank_diagnostic.json`. Gates in `tests/test_pool_optimizer_calibration.py`. *(§2 O6 closed)* |
| 35 | 2026-04-13 | Close O7: tournament-only vs blended+stacked? | **CLOSED — does NOT re-open the pivot.** The tournament-only LOYO experiment was actually run 2026-04-01 (`artifacts/baseline_experiment.json`) but never connected to the ledger. Fresh paired analysis today: `simple_7_logistic` tournament-only gets **paired BSS +4.8%** vs seed baseline across 14 years (paired t p=0.036 uncorrected, Wilcoxon p=0.035, 11/14 year-wins). `fixed_27_logistic` BSS = +1.0% (NS). No config clears Bonferroni at α=0.0125 across the 4 tested. Blended+stacked arm is MEMORY §2 D1: BSS = 0 across production regimes. Head-to-head: tournament-only simple_7 directionally better (+4.8% vs 0%), but §1 Validation lesson ("14-17-yr backtests are underpowered ~9-16% power; use for binary/directional claims, not parameter tuning") means this is evidence, not verdict-overturning. The 2026-04-02 pivot was driven by pool-EV argument (pool optimization is the right axis of improvement) which is unaffected by a ~5% BSS edge. Evidence: `artifacts/o7_regime_comparison_2026-04-13.json`; gates in `tests/test_o7_regime_comparison.py`. Also added MEMORY §3 Baseline Registry row for the tournament-only BSS. *(§2 O7 closed)* |
| 36 | 2026-04-13 | Close O11: seed baseline not zero-information? | **CLOSED — invariant locked.** Council session 25 settled the philosophical claim in transcript ("BSS ≈ 0 is not fatal to game-theory pool optimization because seed baseline is not zero-information"), but no code-level test asserted it. Today's `tests/test_seed_baseline_informativeness.py` (6 tests) locks the concrete properties: extreme seed gaps favor top seed (P(1 vs 16) ≥ 0.85; today 0.99), near-equal seeds near coin-flip (P(8 vs 9) ∈ [0.45, 0.60]; today 0.515), seed-probability spread ≥ 0.30, monotonic in gap on average, seed-lookup Brier < 0.25 with BSS > 10% vs uniform-random (today: 0.213 / +14.8%), and symmetry P(a,b) + P(b,a) = 1. The pool optimizer consumes this 14.8% of seed-embedded information regardless of the ML path. *(§2 O11 closed)* |
| 37 | 2026-04-13 | Close O9: round-specific BSS decomposition? | **CLOSED — ceiling is non-uniform but intact.** `scripts/backtest_by_round.py` output was on disk (`backtest_by_round_results.txt`, 17 yrs × 1071 games) but never serialized or locked. Today's analysis: upset rate varies from 5.9% (NCG) to **42.6% (E8)**; later rounds (S16+) average 29.8% vs early rounds 24.5% — seed dominance weakens. BUT per-round BSS of SI-adjusted vs seed: every powered round shows \|BSS\| < 0.02. E8 specifically (highest upset rate) has BSS = −0.010 — SI is slightly WORSE than seed there. Later-round SI-quintile upset-rate difference is +0.2pp (flat). The non-uniformity exists but doesn't open an exploitable prediction-accuracy gap. Evidence: `artifacts/o9_round_decomposition_2026-04-13.json`; gates in `tests/test_o9_round_decomposition.py` (7 tests). Consistent with §3 row 7 pivot. *(§2 O9 closed)* |
| 38 | 2026-04-14 | Close O17: researcher-DoF leakage audit? | **CLOSED — documented + locked.** Audited the full Optuna tuning surface (LightGBM 8 params, XGBoost 9 params, Logistic 2 params, budget `optuna_n_trials=15 / enable_stacking=False / temporal_cv_splits=5`, and the chronological expanding-window CV policy) + year-visibility table (train: 2008-2024 ex 2020; holdout: [2025]). Calibration-years include 2025 but this is defensible because baseline models train on regular-season games only (tournament 2025 is OOS from base-model training). Evidence committed at `artifacts/o17_researcher_dof_audit_2026-04-14.md`; lock test at `tests/test_researcher_dof_audit.py` (6 tests) fails on any budget or search-space drift. Runtime enforcement piggybacks on the O20 closure (`YearSplitPolicy.assert_dev_artifact_years` invoked from `_enforce_dev_only_years` at `hyperparameter_tuning.py:51`). Residual risk documented: pre-2026-04-07 history opaque (big-bang import commit collapsed the git graph) + no multiple-test correction across 150 per-fit Brier evaluations. *(§2 O17 closed)* |
| 39 | 2026-04-14 | Convert repo to rebase workflow? | **Done.** All 8 `gh pr merge` calls across the 7 auto-merge workflows now use `--rebase` (was mixed `--squash` / `--rebase`). CLAUDE.md adds a Git Workflow section documenting the rebase-first convention and local setup commands (`pull.rebase true`, `branch.autosetuprebase always`, `rerere.enabled true`). Linear history is now the contract across every automated PR path. |
| 40 | 2026-04-14 | Close O12: scoring-function structure in optimizer objective? | **CLOSED — already satisfied in code, now locked forward.** `PoolEnvironment.scoring_rules: Dict[str, int]` is a mandatory env field; `validate()` rejects empty. `PoolOptimizer` propagates to `LeverageCalculator` (3+ sites) and `bracket_portfolio` (1 site). Per-round points enter the EV objective via `pts = scoring_system.get(round, 0)` → `expected_points += ev` at `leverage.py:1618+` — each of R64/R32/S16/E8/F4/CHAMP has its own call site. Three parametric adapters (standard/flat/upset_bonus) in `get_scoring_adapter`; they produce distinguishable leverage outputs so choosing a schedule has observable effect. `AssumptionsManifest` records the schedule with every recommendation. Evidence: `artifacts/o12_scoring_structure_audit_2026-04-14.md`; 7-row regression matrix locked in `tests/test_pool_scoring_structure.py` (8 tests, ~0.1s). *(§2 O12 closed)* |
| 41 | 2026-04-14 | Close O19: pre-2011 ESPN picks — exclude, down-weight, or keep? | **CLOSED — keep archives, exclude from aggregated calibration, make boundary enforceable.** Data analysis: pre-2011 champions 3/3 one-seeds (mean 1.00) vs post-2011 1.86 (3 of 14 non-1); public over-picks seed-1 at S16/E8/F4/CHAMP by 14.5/12.6/8.4/4.9 points — a regime shift consistent with field expansion 64→68 + analytics-culture onset. Pooling pre-2011 with post-2011 in opponent-model calibration would skew the model chalky for predicting post-2011 pools. Status quo was already compliant (`SEED_PICK_RATES` 2015-2024; `mc_pool_backtest.py` starts 2011) but implicit. Code change: `MIN_PICKS_CALIBRATION_YEAR = 2011` constant + `strict_post_2011` kwarg on `load_historical_public_picks` (raises on pre-2011 under strict mode). Archived JSON files stay on disk for single-year simulation (2008 sim uses 2008 picks). Evidence: `artifacts/o19_pre_2011_picks_audit_2026-04-14.md`; 8 tests at `tests/test_pre_2011_picks_gate.py`. *(§2 O19 closed)* |
| 42 | 2026-04-14 | Close O13: WTA Kelly-variance vs argmax-P(1st)? | **CLOSED — stochastic wins; production already locked.** Mined the 2026-04-12 MC backtest (`mc_pool_backtest_n31_det_vs_stoch.txt`, 13 yrs × N=31 × 50 brackets × 50 repeats) for the head-to-head. Stochastic `*_first_tv` BestRank = 1.52 vs argmax `det_*` BestRank = 9.92; stochastic wins 10/13 years, argmax wins 3 (all chalky: 2015/2019 + one tie). Argmax higher aggregate P(1st) (0.060 vs 0.041) is illusory — bimodal with zeros in 7/13 years. Kelly framing (log-utility, finite downside) correctly favors stochastic; argmax's aggregate benefit requires pre-knowing chalky years. Production recommendation already `champ_first_tv` (stochastic) per MEMORY.md §1; closure documents the evidence and locks the choice. Evidence: `artifacts/o13_kelly_vs_argmax_audit_2026-04-14.md` + `artifacts/o13_kelly_vs_argmax_2026-04-14.json`; 7 lock tests at `tests/test_kelly_vs_argmax_lock.py`. New MEMORY.md §2 D12 dead-end row for deterministic-argmax pool construction. *(§2 O13 closed)* |
| 43 | 2026-04-14 | Close O14: calibration-to-pool interaction (upset-over-estimation)? | **CLOSED — gate hypothesis falsified.** Temperature sweep T ∈ {0.5, 0.75, 1.0, 1.25, 1.5, 2.0, 3.0} × 13 years × 200 stochastic brackets. Brier-optimal T = 1.0 (honest calibration). P(beats chalk) *monotonically decreases* in T: 0.236 at T=0.5 → 0.224 at T=1 → 0.194 at T=3.0 — opposite direction of the "upset-over-estimation helps pool rank" gate prediction. Calibration-to-pool interaction exists (best-score ties at T=0.5 and T=1.5 at 914 vs T=1's 909) but magnitude is trivial (≤5 ESPN pts, ≤2 pp pBeatChalk) and the dominant lever is stochastic sampling (O13). Keep T=1 honest calibration; don't introduce a second calibration temperature. New MEMORY.md §2 D13 dead-end. Evidence: `scripts/o14_calibration_sweep.py`; `artifacts/o14_calibration_sweep_audit_2026-04-14.md` + `.json`; 6 lock tests at `tests/test_calibration_pool_interaction_lock.py`. *(§2 O14 closed)* |
| 44 | 2026-04-14 | Lock O4: opponent-independence verdict (re-verify + drift guard)? | **LOCKED.** O4 was narrative-closed 2026-04-13 via `ANALYSIS_O4_OPPONENT_CORRELATION.md` but had no lock test — the core independence invariant lived only in prose. Re-ran `independence_test` across 4 years (2023-2026 × 93 brackets, rng_seed=42, n_sim=2000/year), serialized per-year z-scores to `artifacts/o4_opponent_independence_2026-04-14.json`. Stouffer pooled z = **−4.15** (identical to the ANALYSIS doc). Per-year: −2.38 / −2.18 / −1.82 / −1.91. Drift guard `tests/test_opponent_independence_lock.py` (7 tests) asserts pooled z < −1.0 (margin against recorded −4.15), no year flips to +1.96 clustering, Stouffer formula sanity, aggregate numbers within [−6, −2]. Any future regression that causes brackets to cluster MORE than IID — the exact validity concern the council flagged — will now fail CI rather than being discovered during a live pool. New MEMORY.md §1 Pool-strategy row "Opponent-independence invariant." *(§2 O4 locked, remained closed)* |
| 45 | 2026-04-14 | Close O10 properly: theoretical copula for opponent dependence? | **CLOSED — superseded by O4 lock.** O10 was `[mostly moot 2026-04-13]` because O4 showed independence holds; now that O4 is hard-locked by `tests/test_opponent_independence_lock.py` (pooled z < −1.0 required), the copula work is permanently deferred. Reactivation contract: the O10 row explicitly ties reopening to O4's lock test firing — if `pooled_stouffer_z` ever climbs to ≥ 0 or any single year's z ≥ +1.96, both O4 and O10 reopen together. Until then, "independence + empirical marginals" is the committed opponent-model shape. New MEMORY.md §2 **D14** dead-end row for theoretical-copula opponent modeling; the dead-end is explicitly *conditional* — a data regime change could resurrect it but the code lock makes the trigger observable. No artifact needed since there's no new empirical work; the O4 lock carries the evidence. *(§2 O10 closed)* |
| 46 | 2026-04-14 | Close O15 as conditional-blocked: Vegas-line vs temperature scaling? | **CLOSED — blocked on data access; ROI marginal even if unblocked.** Empirical data audit confirms no historical Vegas lines anywhere in repo (`data/raw/` swept for `vegas/odds/market/spread/moneyline` — zero hits; 7OT/Torvik/Kaggle data are ratings + outcomes only, no market lines). The scraper was deleted per §2 D10 and no replacement lives in the tree. Free re-acquisition paths exhausted (TheOddsAPI historical paywalled at $30-250/mo, Covers/SBRO archives bot-blocked, Kaggle ships outcomes-only). The `market_validation.py` consumer stub is still present and would accept Vegas input the moment it exists. ROI argument: the literature consensus is Vegas ≈ best models at BSS ±0.5-2% on NCAA tournament, which is second-order given §3 row 7 pivot (accuracy → pool EV) + O7 (simple_7 = +4.8%) + O14 (calibration-to-pool interaction is trivial). Reactivation contract: (1) any `data/raw/vegas_lines_{year}.json` lands, OR (2) a future §2 item reopens where accuracy-ceiling is the binding pool-EV constraint (which would flip the row-7 pivot). Same pattern as O10 — conditional dead-end with explicit trigger. New MEMORY.md §2 **D15**. No empirical artifact (there's nothing to measure); the closure is the formalization of the blocker. *(§2 O15 closed)* |
| 47 | 2026-04-15 | Close O22: malformed `tournament_results_2026.json`? | **CLOSED — deterministic relabel, not re-scrape.** Pre-repair counts {R68:4, NCG:49, S16:8, E8:4, F4:2} vs canonical {FF:4, R64:32, R32:16, S16:8, E8:4, F4:2, NCG:1}. Root cause lives in `src/data/scrapers/tournament_results.py::_parse_espn_event`: `_ESPN_ROUND_MAP["championship"]→"NCG"` collapses any ESPN headline containing "championship" but not a more specific phrase onto NCG. The underlying game data (teams/scores/winners) is intact — team-continuity audit shows FF winners ⊆ R64 teams and R64→R32, R32→S16, S16→E8, E8→F4, F4→NCG are exact bijections. Fix: `scripts/repair_tournament_results_2026_o22.py` (preconditions: exact malformed counts + chronological run layout + team-continuity invariants; writes idx 0..3 R68→FF, 4..35 NCG→R64, 36..51 NCG→R32, F4+NCG region=""→"National"). Drift guard: `tests/data_integrity/test_tournament_results_taxonomy.py` — 44 parametrized tests across every year 2005-2026, era-aware canonical counts (1×FF pre-2011, 4×FF 2011+), fails loudly on non-canonical labels. The scraper's fragile substring matcher (silent fallback to R64 default + first-match-wins without specificity ordering) is flagged for follow-up; outside O22 scope. *(§2 O22 closed)* |
| 48 | 2026-04-15 | Structural fix for the O22 scraper root cause? | **CLOSED.** Four coordinated changes to `src/data/scrapers/tournament_results.py`: (1) replaced the `_ESPN_ROUND_MAP` dict with an explicitly specificity-ordered tuple of compiled regex patterns (`_ROUND_PATTERNS`) — most-specific phrase that appears in the joined headline text wins; (2) removed the bare `"championship"` catch-all that was the literal O22 trigger; (3) replaced the silent `round_name = "R64"` default in `_parse_espn_event` with `return None` when no pattern matches, so the event is dropped rather than mislabeled; (4) added post-scrape `_assert_canonical_labels` and `_assert_canonical_distribution` guardrails (raises `TaxonomyValidationError`) invoked in `scrape_year` — catches the O22 shape at scrape time rather than weeks later during year-over-year analysis. Emits canonical `FF` instead of `R68`. Matcher is a pure function (`_round_name_from_headlines(headlines) -> Optional[str]`) with 23 unit tests at `tests/test_tournament_results_scraper_taxonomy.py` covering: specificity ordering (O22 literal case "Championship - First Round" → R64, not NCG), note-order invariance, digit/word form equivalents ("Elite 8" ≡ "Elite Eight"), explicit `None` on no-match, bare-championship-does-not-leak-to-NCG, both guardrails raise on the exact O22 defect shape, partial scrape skipped so mid-tournament polls don't false-fail. All 156 pre-existing scraper tests still green. The distribution asserter is era-aware (pre-2011 expects 1×FF; 2011+ expects 4×FF) and total-count-gated (skipped when `len(games) != 67/64`). Follow-up work deferred: the Sports Reference HTML fallback (`_parse_region` / `_ROUND_LABELS`) has no FF handling and still emits column-position labels; fine because SR is fallback-only and the post-scrape guardrails would catch any drift at scrape time. |
| 49 | 2026-04-15 | Close §1 Diversity / §3 row 17 diversity-collapse? | **CLOSED — two observations with different resolutions, neither a live bug.** The council bullet bundled two distinct claims under one label. **Observation A:** `ParetoOptimizer.generate_pareto_brackets` returns 5–8 unique brackets in production forecast reports (10–13 for `det_*` paths) — matches the "8–14 unique brackets" symptom. This is intended per the docstring: "May return fewer if the data genuinely has fewer distinct credible brackets." The risk sweep honestly reflects the (probability × differentiation) curve, which has few crossovers when one team is an undervalued favorite (Michigan 2026 mod 20.0% vs public 13.9%; Gonzaga 2017). Opt-in remediation `include_champion_augmentation=True` forces per-champion variants; default OFF per user preference. **Observation B:** the "stochastic-tilt made mean rank worse (620 vs 532)" regression is the `leveraged` mode aggregate (MeanRnk 620.2) vs `seed` (532.4) across 17 yrs in `mc_pool_backtest_equalized_results.txt:272,275` (paired t=−3.808, p=0.0015, wins 3/17). The producing modes (`leveraged`, `opt_seed`, `opt_blend`, `opt_torvik`, `hedge_tv`) were retired from `mc_pool_backtest.ALL_MODES` per MEMORY.md §2 D6/D7. Current `ALL_MODES` = (seed, noseed, blend, torvik, champ_first_tv, f4_first_tv, e8_first_tv, det_champ_tv, det_f4_tv, det_e8_tv) — 10 modes, zero deprecated. The stochastic-tilt utility `build_leverage_tilted_round_probs` has **zero callers anywhere** in the repo; `build_optimized_brackets` has one allowlisted caller (`scripts/diagnose_catastrophic_years.py`, a pre-pivot `opt_torvik` diagnostic) and zero `src/` callers. Evidence: `scripts/diversity_collapse_diagnostic.py` → `artifacts/diversity_collapse_2026-04-15.json`. Drift guard: `tests/test_diversity_collapse_lock.py` (6 tests): pins `ALL_MODES` membership, fails if any dead mode reappears, fails if either dead utility gains a `src/` caller or (for the tighter gate) any caller whatsoever, pins the `ParetoOptimizer` docstring language that documents Observation A as design. Third council observation ("Mean rank vs P(top 5%) tell different stories") is already locked forward by the 2026-04-02 pivot (MEMORY.md §1 Strategic pivot) — production target is P(1st) + P(top 5%), not mean rank. |
| 50 | 2026-04-15 | Close §1 Data pipeline / artifact provenance? | **CLOSED — response-hashing + manifest-ledger half landed; request-logging half deferred.** New utility `src/data/data_provenance.py` (re-uses `manifest_generator._sha256_file`) computes a content-hash ledger keyed by the manifest's logical artifact name; idempotent. `scripts/audit_data_provenance.py` walks every `data/raw/manifest_{year}.json` and merges the ledger under `provenance.data_artifacts` (preserves any pre-existing provenance keys like 2026's `data_quality_repair` block). Production manifests populated 2026-04-15: 2025 = 3 artifacts (1 missing on disk; flagged via `MISSING_SENTINEL`), 2026 = 7 artifacts. Module placed at `src/data/` not `src/governance/` because `src.governance.__init__` eagerly pulls the pandas-heavy `production_validator` import chain via `src.pipeline.config` — `src.data.__init__` is empty so the new module is importable in lightweight CI envs. Drift guard: `tests/data_integrity/test_data_provenance_audit.py` (14 tests): contract (every production manifest has a populated ledger; every artifact has a record; required fields present), drift detection (recorded sha256 must equal a freshly-computed sha256), idempotency (two consecutive computes produce identical hashes), sentinel handling (missing files use `MISSING_SENTINEL`, don't crash), coverage (every `manifest_*.json` on disk is enrolled in `PRODUCTION_AUDIT_YEARS`). **Deferred half:** request logging — URL / fetch-time / response-header recording at scrape time. That requires touching every scraper (BartTorvikScraper, TournamentResultsScraper, ESPNPicksScraper, …) and is independent of what landed here; explicitly NOT in scope for this closure. The existing `src/governance/artifact_provenance.py` (used at the prediction-run layer) is unchanged. |
| 52 | 2026-04-16 | Does pool_hist_results.json need to feed the optimizer, or is P(score ≥ 1300) sufficient? | **CLOSED — FAIL. Opponent simulation is load-bearing.** pool_hist_results.json is already NOT in the production path (O21 null result). The score-threshold question (O24) was novel and has now been tested: ρ = 0.20 / 0.05 / 0.56 across 2023–2025, far below the 0.95 gate. Root cause: the 97th-percentile of max-over-60-bracket scores ≈ 1360 pts is unreachable for any individual bracket (p_threshold ≈ 0.000–0.003), so rankings collapse to noise. pool_hist_results.json remains valuable for validation (tests/test_pool_hist_ev_validation.py). The opponent simulation itself must not be replaced with a score threshold. *(§2 O24 closed — FAIL)* |
| 53 | 2026-04-16 | How to build a bracket selection optimizer? | **The correct objective is P(b_i > max(opponents)), not P(rank=1) in isolation.** Current ρ=0.37 (O6) is structurally weak — 86% noise — but the weakness is in the objective function, not a tuning problem. O4's anti-correlation (z=−4.15) means max(opponents) has *higher* variance than the IID case, systematically favoring high-upside upset brackets; the current ranker underweights this because it models opponents as near-IID. **Two prerequisite diagnostics before implementation:** (1) spread diagnostic — if P(rank=1) spread between #1 and #11 in 2026 is < 1%, selection optimization is noise trading and the real bottleneck is portfolio generation diversity; if > 3%, the ranker has a fixable systematic blind spot. (2) 2026 post-mortem — bracket #11 won; it is 25% of all labeled data; determine whether #11 had a structurally distinct upset profile that the ranker discounted. **Rejected approaches (council + peer review):** Outsider classifier (only 4 positive examples across 200 observations, not viable); Executor conditional clustering (risks re-litigating D6/D16). **Opens §2 O25** with three dependency-ordered gates. Transcript: `council-transcript-2026-04-16-bracket-selection.md`. |
| 51 | 2026-04-15 | Promote §1 Pool-strategy chalk-bias bullet to §2 with a falsifiable gate? | **DONE — bookkeeping promotion, not implementation.** §1 chalk-bias bullet's `[partially actioned]` tag was the last orphan status in §1 with no §2 Ox row. That made the item invisible to the §4 Pre-Council Duplicate-Check Protocol (Bucket B requires a §2 hit) and left "what does done mean" undefined. Promoted as **§2 O23** with five concrete gates (G1 empirical bias table from `data/raw/historical_public_picks/`; G2 construction-layer chalkfade sampler; G3 13-yr head-to-head backtest in `mc_pool_backtest.py`; G4 paired t-test with Bonferroni α=0.0167 across BestRank / P(1st) / P(top 5%); G5 O21-style ranking-stability cross-check). Three documented failure modes the gate explicitly forbids re-tripping: **D8** (round-agnostic top-quintile contrarian, lost 7.1% to chalk over 853 matchups); **§1 F4-cap warning** (small-sample tuning of cap value from 14-yr historical avg); **O21** (marginal-shift-only changes that move absolute P(1st) but not bracket *ranking*). Closure rule pre-committed: all 5 gates pass → lock new mode in MEMORY.md §1 Pool-strategy + add lock test; any gate fails → archive evidence + add MEMORY.md §2 D16 dead-end row. **Deliberately NOT implementing the exploit in this row** — the implementation is what O23 gates. §1 status crumb refreshed `[partially actioned]` → `[promoted to §2 O23 on 2026-04-15]`. *(§1 Pool-strategy chalk-bias bullet promoted; §2 O23 added)* |
| 54 | 2026-04-16 | Close O25 (empirical-supremum bracket selection) via real G1 + G2 runs? | **CLOSED as dead-end D17.** G2 FAIL under both shape-encoded and team-identity (real ESPN) scoring: mean ρ(p_sup, actual) across 4 yrs = +0.0605 (shape), +0.0086 (team-identity); baseline ρ(p_rank1, actual) = −0.050 (shape), +0.066 (team-identity). Gate was > 0.50; fails by an order of magnitude under both encodings. G2-B (actual-#1 flips into top-5): 0/4 years under either scoring. **Under team-identity the OLD within-portfolio P(rank=1) is marginally BETTER than the proposed supremum** — the change makes things worse, not better, under the scoring that actually determines pool payouts. **Coverage is not the bottleneck:** under team-identity scoring the 50 candidates contain a bracket that beats the stored pool winner pts in ALL 4 years (2023 1220>720, 2024 1400>1380, 2025 1660>1600, 2026 1520>1350) — selection, not generation, is the limiting factor. **Secondary finding:** prior G1 narrative in row 229 (spreads 0.835/0.315/0.045/0.310) was undocumented and unreproducible — no script, no artifact, inconsistent with pre-existing `rank_correlation_diagnostic.json`. Real G1 re-run lands 9/12 spread measurements in the AMBIGUOUS 1%-3% band. **Encoding-invariant verdict:** the shape-encoding caveat is not load-bearing; team-identity rerun gives the same FAIL conclusion. Outputs: `scripts/o25_g1_spread_diagnostic.py`, `scripts/o25_g2_empirical_supremum.py`, `scripts/o25_g2_team_identity.py`, `artifacts/o25_g1_real_2026-04-16.json`, `artifacts/o25_g2_empirical_supremum_2026-04-16.json`, `artifacts/o25_g2_team_identity_2026-04-16.json`, `tests/test_o25_g1_g2_lock.py`, `src/simulation/pool_history_opponent_model.py::pool_entry_to_bracket_vector`. *(§2 O25 closed — D17, amended with team-identity companion run)* |
| 55 | 2026-04-16 | Scoring-function divergence audit (`score_brackets_against_outcome` shape encoding vs real ESPN team-identity): is shape a reasonable proxy? | **OPENS §2 O26.** Investigation prompted by D17 surfaced that pipeline-wide shape encoding (`(brackets == outcome) @ scoring_vector`) isn't what the actual ESPN pool uses for payouts. Spot-check (`scripts/o26_rank_corr_team_identity_spotcheck.py` → `artifacts/o26_rank_corr_team_identity_spotcheck_2026-04-16.json`): over 2023-2026 × ~10-19 brackets/year, mean ρ_shape = +0.049, mean ρ_team_identity = **+0.491**. In upset-heavy years the sign inverts: 2023 ρ_shape = −0.25 but ρ_team = **+0.93**; 2024 ρ_shape = −0.28 but ρ_team = **+0.78**. mean \|Δρ\| = 0.68 — **not a rounding issue**. Implications: O6's mean ρ=+0.37 claim likely under-states the real P(1st) signal; §3 Pool backtest table + O21/O24/D12 closures are all shape-encoded and may shift under team-identity. Opens §2 O26 with four gates (full 14-yr rank_corr rerun → per-closure audit → production backtest rerun → optional code migration). Advisory: hold council work that references shape-encoded MEMORY numbers until O26-G1 runs. *(§2 O26 added; not yet gated)* |
| 56 | 2026-04-17 | O26-G1: full 14-yr rank_correlation rerun under team-identity? | **AMEND REQUIRED — O26-G2 promoted from optional to required.** `scripts/o26_g1_full_14yr_rerun.py` → `artifacts/o26_g1_full_14yr_rerun_2026-04-17.json`. 15 years (2011-2026 ex 2020) at N_PLACEMENT_TRIALS=200 matching the O6 baseline exactly. Shape companion: mean ρ = **+0.361** (median +0.422, 87% positive, t=+3.55 p=0.003) — reproduces O6's recorded +0.37 within 0.01, confirming the original methodology was shape-encoded. Team-identity: mean ρ = **+0.613** (median +0.688, **100% positive across all 15 years**, t=+8.53 p<1e-6). \|mean Δρ\| = 0.252, exceeding the pre-committed 0.15 amend threshold by 70%. MEMORY.md §1 O6 row amended to record both numbers. Per-year highlights: 2011 +0.90 Δ, 2023 +1.39 Δ, 2025 +0.54 Δ (biggest sign flips in upset-heavy years). The "+0.37 / 86% noise" framing cited in §3 row 53 (when O25 was opened) is now known to be an encoding artifact — under real scoring, P(1st) explains ≈ 37% of rank variance, not 14%. G2 (per-closure audit for O21/O24/D12) is now mandatory before those closures can be cited; G3 (production MC backtest rerun) still open; G4 (code migration) still optional. *(§2 O26 G1 complete; MEMORY.md §1 O6 amended)* |
| 57 | 2026-04-17 | O26-G1b: is O26-G1's +0.61 also valid against the user's ACTUAL 30-person pool (not ESPN-national opponents)? | **MIXED — CALIBRATION IS POOL-SPECIFIC.** `scripts/o26_g1b_pool_scoped.py` → `artifacts/o26_g1b_pool_scoped_2026-04-17.json`. Scope: 2023-2026 (the years with pool_hist_results.json). Team-identity scoring, actual 30-person pool as the fixed opponent field, placement rank computed deterministically vs actual outcome. Per-year ρ(P(1st), −placement): 2023=**+0.97**, 2024=+0.36, 2025=**+0.97**, 2026=**−0.60**. Mean ρ = **+0.42**, median +0.66, 75% positive (3/4), t=+1.14 p=0.338 (not significant with n=4). Compared to O26-G1's ESPN-national aggregate (+0.613, 100% positive across 15 yrs), the pool-scoped calibration is weaker and has a real sign-flip year. **The 2026 inversion is load-bearing:** high-P(1st) portfolio brackets placed WORSE than low-P(1st) brackets in 2026 — consistent with the §3 row 23 / row 25 narrative ("system ranked its own winning bracket #11"), and evidence that chasing P(1st) would not have helped in 2026. MEMORY.md §1 O6 row further amended with a scope note + both scopes' aggregates. **Implication for opponent-model work:** the ESPN-national opponent field is a cleaner calibration target than the actual pool, meaning opponent-model mis-specification (pool ≠ ESPN-national) has real directional impact on P(1st) reliability. **Does NOT invalidate the O26-G1 amendment** (ESPN-national scope remains at ρ=+0.61); DOES narrow the scope under which "+0.37 → +0.61" can be cited. *(§2 O26 G1 + G1b complete; MEMORY.md §1 O6 scope-amended)* |
| 58 | 2026-04-17 | 2026 post-mortem: did the system have a pool-winning bracket in its portfolio, and did it select the right one? | **CONFIRMED — generation is fine; selection is the 2026 bottleneck.** Derived from `artifacts/o26_g1b_pool_scoped_2026-04-17.json` 2026 row. The 19-bracket portfolio contained **8 brackets that placed #1** (tied for best) against the actual 30-person pool under team-identity scoring — the correct bracket was generated multiple times. BUT the 3 brackets with HIGHEST P(1st) (0.0415, 0.0385, 0.034) placed **#10, #10, #19** respectively; the 8 #1-placers had P(1st) values in [0.014, 0.024] — the LOWER end of the distribution. A P(1st)-maximizing selector in 2026 would have picked a bracket that placed #10 while the portfolio contained multiple bracket-copies that would have won. Empirical, point-identified support for MEMORY.md §3's "system produced a winning-quality bracket ... but ranked it #11 in its own portfolio" claim; narrows the 2026 failure to the selection layer. Because O26-G1b shows P(1st) is well-calibrated (ρ=+0.97) in 2023 and 2025 and fails only in 2026, the residual error is almost certainly opponent-model mis-calibration to THIS pool in THIS year, not a ranker design flaw. **Implication for 2027 priority order:** (a) opponent-model calibration upgrades outrank (b) selection-objective search (O25-style). The D17 closure of O25 is the correct call given this evidence — the empirical supremum was not going to fix what is really an opponent-model problem. *(Indexes finding; no new §2 item opened pending opponent-model proposals — tracked in §1 "Opponent model weights" row as the next upgrade surface.)* |
| 59 | 2026-04-17 | O26-G2a: does the O21 null result hold under team-identity scoring? | **PARTIALLY REOPENS O21 — signal is not encoding-invariant, but the production lever remains modest.** `scripts/o26_g2_o21_team_identity.py` → `artifacts/o26_g2_o21_team_identity_2026-04-17.json`. O21's original flat result (ρ ≈ +0.42-0.46 across all blend weights, "marginals don't change rankings") was an artifact of the mixed-encoding bug in the original script: pool brackets were scored team-identity via `score_pool_bracket` while synthetic opponents were scored shape via `score_brackets_against_outcome`, making per-weight P(1st) estimates inconsistent. Consistent team-identity scoring (applied to both sides) across 2023-2025 × 5 blend weights × 400 sims × K-1 per-year opponents produces **mean ρ = +0.709 / +0.767 / +0.627 / +0.718 / +0.712** at w=0.00/0.25/0.50/0.75/1.00. All weights show ρ substantially higher than shape's flat ~+0.45 (Δρ vs shape: +0.17 to +0.31). Argmax best-weight flips from w=0.00 (ESPN-only, shape era) to **w=0.25** (modest pool blend, team-identity) but the advantage over ESPN-only is +0.058 ρ on n=3 yrs, K=18/25/32 — visible signal, insufficient to justify a production blend-weight change. §2 O21 marked `[closed 2026-04-13; partially reopened 2026-04-17]`; MEMORY.md §1 opponent-model-weights row unchanged; flagged as a candidate-generation upgrade surface per §3 row 58. Drift guard: `tests/test_o26_g2_lock.py`. *(§2 O21 amended; O26-G2 1/3)* |
| 60 | 2026-04-17 | O26-G2b: does the O24 FAIL verdict hold under team-identity scoring? | **ENCODING-INVARIANT FAIL — strengthened.** `scripts/o26_g2_o24_team_identity.py` → `artifacts/o26_g2_o24_team_identity_2026-04-17.json`. Team-identity scoring applied to both model-bracket and opponent-bracket scores against each of 5000 MC tournament outcomes, 30 candidates vs 30 opponents per year across 2023-2025. Per-year ρ(p_rank1, p_threshold): **0.31 / 0.16 / 0.34**; top-decile concordance: **0.00 / 0.33 / 0.00**. Every year fails both gates (ρ ≥ 0.95, concordance ≥ 0.97). The FAIL is stronger than shape (2025 ρ drops from 0.56 to 0.34; 2023 and 2025 top-decile concordance collapse to 0). **Root cause unchanged** — the 97th-percentile winner-score threshold is ~1560 pts under team-identity (vs ~1360 shape), still unreachable for any individual bracket (p_threshold ≈ 0.000-0.003). A bracket wins by being max-scorer in a trial, not by crossing a high absolute threshold; the joint tournament realization structure is what the simulation captures and the threshold discards. Do not replace the opponent simulation with a score threshold. §2 O24 marked `[closed 2026-04-16 — FAIL; encoding-invariant 2026-04-17]`. Drift guard: `tests/test_o26_g2_lock.py`. *(§2 O24 amended; O26-G2 2/3)* |
| 61 | 2026-04-17 | O26-G2c: does the D12/O13 stochastic-vs-argmax verdict hold under team-identity scoring? | **ENCODING-INVARIANT — STRENGTHENED.** `scripts/o26_g2_d12_team_identity.py` → `artifacts/o26_g2_d12_team_identity_2026-04-17.json`. Narrow-scope D12 replication (2 modes: `champ_first_tv` stochastic vs `det_champ_tv` argmax; N=31 pool; 50 brackets × 50 repeats; 14 years 2011-2025 ex 2020). Under team-identity: stochastic aggregate BestRank **1.79** vs argmax **7.44** — direction preserved from shape's 1.52 vs 9.92. **Per-year BestRank winner: stochastic sweeps 14/14 years under team-identity (vs shape's 10/13).** Argmax P(1st) marginally higher on aggregate (0.035 vs 0.034) but entirely carried by the 2019 chalky-year spike (argmax P(1st)=0.39 vs stoch 0.08) with zeros in 9 of 14 years — matches D12's documented "bimodal chalky-year wins" narrative. Kelly / concave-utility framing still favors stochastic's consistency over argmax's bimodal catastrophe risk. §2 D12 (MEMORY.md) marked encoding-invariant. Drift guard: `tests/test_o26_g2_lock.py`. *(MEMORY.md §2 D12 amended; O26-G2 3/3 — G2 complete; G3/G4 still open)* |
| 62 | 2026-04-17 | O26-G3 (narrow, N=31): do production MC backtest mode-rankings hold under team-identity? | **MATERIAL REORDERING; ESCALATION DEFERRED TO O27.** `scripts/o26_g3_mc_backtest_n31_team_identity.py` → `artifacts/o26_g3_n31_team_identity_2026-04-17.json`. 9 of 11 modes (noseed+blend skipped — ML stack not in sandbox), N=31 pool, 50 brackets × 50 repeats, 14 yrs (2011-2026 ex 2020; 2012 skips on missing ESPN picks), ESPN opponents. Shape baseline = POOL_STRATEGY_RECOMMENDATION.md:148-154 (2026-04-12 run). **BestRank top-5 reorder:** shape f4_first_tv (1.50) / e8_first_tv (1.50) / champ_first_tv (1.60) / seed (1.60) / torvik (2.00) → team-identity **torvik (1.42)** / e8_first_tv (1.87) / f4_first_tv (2.06) / seed (2.30) / **champ_first_tv (2.67)**. Production-recommended `champ_first_tv` goes from BestRank ~tied for top to worst of the 5 headline modes under team-identity. Team-identity scoring rewards torvik's per-game accuracy advantage (+4.8% BSS per §2 O7) more than shape does — shape collapsed the modes into a ±0.10 band; team-identity separates them by 1.25 rank positions. **P(1st) argmax PRESERVED** — torvik wins both shape (0.0445) and team-identity (0.0417), so the WTA winner-take-all verdict is not invalidated. **`det_*` modes show extreme team-identity bimodality** — `det_e8_tv` 2026 P(1st)=0.400 carries aggregate to 0.0717, but BestRank 7.30 (poor); consistent with D12's encoded caution. Per pre-committed gate, this would trigger N=1000 escalation. **User decision 2026-04-17: defer N=1000 rerun to new §2 O27.** G3-narrow stands as authoritative evidence at N=31 small-pool scope; MEMORY §3 Pool backtest table (N=1000) remains shape-encoded but carries a team-identity caveat. Drift guard extension to `tests/test_o26_g2_lock.py`. *(§2 O26 status advanced to G3-narrow complete; §2 O27 opens for N=1000 rerun)* |
| 63 | 2026-04-17 | O26-G4 companion: add the team-identity scorer to `src/simulation/pool_competition.py` without migrating MC call sites. | **LANDED (companion only; MC migration gated on O27).** `src/simulation/pool_competition.py` gains three public functions peer to the shape scorer: `score_brackets_team_identity(brackets, winners_by_round, first_round, scoring_system)`, `picks_by_round(vec, first_round)`, `actual_winners_by_round(games)`. Shape scorer (`score_brackets_against_outcome`) is unchanged in behavior; its docstring now carries an explicit ENCODING WARNING + pointer to the companion + O26 reference. Rationale for NOT migrating call sites now: the MC-based path (`scripts/mc_pool_backtest.py`) is the producer of MEMORY §3's Pool backtest table numbers; swapping its scorer without simultaneously re-running at N=1000 would leave the MEMORY table stale-shape-encoded while the code produced team-identity — an inconsistency worse than the current caveat state. O27 (N=1000 rerun) is the right time to re-anchor both the numbers AND the call site. **What this closure buys:** (1) future scripts have a named, documented team-identity scorer to import instead of duplicating the 4-copy `_picks_by_round` / `_team_identity_scores` helpers currently living in `scripts/o26_g1_full_14yr_rerun.py`, `scripts/o26_g2_o21_team_identity.py`, `scripts/o26_g2_o24_team_identity.py`, `scripts/o26_g2_d12_team_identity.py`, `scripts/o26_g3_mc_backtest_n31_team_identity.py`, `scripts/o25_g2_team_identity.py`. (2) the docstring encoding-warning closes the bug-class that caused G2-a's original O21 script to mix encodings (pool brackets team-identity, opponents shape). (3) lock tests assert the companion API + docstring + signature can't silently regress. Tests: 8 new in `tests/test_pool_competition.py::TestTeamIdentityScoring` (including a hand-computed shape-vs-team-identity divergence case that isolates the pure encoding gap: shape delta +20 vs team-identity delta 0), 4 new in `tests/test_o26_g2_lock.py`. *(§2 O26 status advanced to "G1+G2+G3-narrow+G4-companion complete"; O27 remains open for N=1000 + MC migration)* |
| 64 | 2026-04-17 | Next 11-month direction: continue opponent-model track (O27 → O21) or pivot? | **PIVOT TO FREE MEASUREMENTS FIRST.** Bucket-E 5-advisor panel, peer-reviewed. 4 of 5 advisors (Contrarian / First-Principles / Statistician / Outsider) flag a fatal flaw in the status-quo "continue the roadmap" plan: §3 row 58's "opponent-model > selection" claim is **circular** — rests on n=4 pool-years with 2026 as the motivating datapoint. Peer review 3 of 5 picks Outsider as strongest (reframes problem around actual objective: win a 30-person pool once); 2 of 5 pick First-Principles (compute the irreducible noise floor before building anything). Chairman's independent finding: before O27, before noise-floor analysis, before opponent scraping — **pull the 2026 realized ESPN pool standings and compute where each of the 50 portfolio brackets would have finished**. This is an afternoon against already-collected data and either dissolves the problem (submitted bracket won) or localizes it (ranker's #11 was systematically beaten by internal #12-#20). Three dependency-ordered actions: (1) pull realized 2026 pool standings — if submitted bracket placed top-3, the 11-month calibration plan needs re-justification from scratch; (2) run O27 tonight in parallel as bookkeeping, log result to MEMORY as directional only (do NOT let it reorder priorities regardless of outcome); (3) compute irreducible noise floor (First-Principles proposal) — given perfect opponent knowledge and n=3-4 pool-years, what is the best achievable BestRank? If the ceiling gap between "current system" and "perfect opponent model" is <3 rank positions, kill the opponent-calibration track and pivot to pool-rules verification + entry-count leverage. **Load-bearing caveats:** (a) Scoring encoding — invalidated if O27 flips sign; (b) Opponent field — invalidated if >30% 2027 roster churn; (c) Year scope — n=4 already invalidated for ranking claims (Statistician 95% CI on 75% positive was [19%, 99%]); treat all pool-scoped claims as directional; (d) Entry-count leverage observation (25 ESPN brackets) is oversold — verify pool rules before treating as 25× lever. Transcript: `council-report-2026-04-17-2307-next-11-months.md`. *(No new §2 item opened; action 1 and action 3 are new measurement tasks against existing data/code.)* |

---

## 4. Pre-Council Duplicate-Check Protocol

**Contract for the `llm-council` skill:** before spawning 5 advisor sub-
agents, the skill MUST run the checks below. Most aborts are cheap
("already answered"); one class is genuinely useful ("identified but
unaddressed — do you want to execute or re-debate?").

### Step 1 — Load this file's §1, §2, §3
Read the whole file. It is < 500 lines; cost is negligible vs. spawning 11
sub-agents.

### Step 2 — Classify the user's question
Map it to one of these five buckets:

| Bucket | How to detect | Action |
|---|---|---|
| **A. Already answered (§3 duplicate)** | Question semantically matches a row in §3 (e.g. "what's the biggest repo limitation?" was asked 6× — rows 1, 2, 4, 5, 6, 20). | **Do not spawn advisors.** Tell the user: "Council answered this on `<date>`: `<verdict>`. Re-run only if you have new evidence that invalidates the prior verdict." Ask one clarifying question: new evidence, or different framing? |
| **B. Already-identified open item (§2 hit)** | Question matches an `Ox` item the council has already named — user is asking "what should we do about X" where X is in §2. | **Do not spawn advisors.** Surface the specific row: "§2 O`<n>` already identifies this. Gate: `<gate>`. Status: `<status>`. The work is execution, not debate." Ask: do you want help executing, or is the gate itself wrong? |
| **C. Blocked-on prerequisite** | Question touches an `Ox` that has `blocked:O<m>` status. | **Do not spawn advisors.** Tell the user: "O`<n>` is blocked on O`<m>`. Current status of O`<m>`: `<status>`. Unblock that first." |
| **D. Locked decision (MEMORY.md §1)** | Question proposes changing something locked in MEMORY.md §1 ("should we switch from `champ_first_tv`?", "enable stacking?"). | **Do not spawn advisors yet.** Cite the MEMORY row + source. Ask: is there new evidence that invalidates the lock? If yes → proceed to Step 3. If no → stop. |
| **E. Genuinely novel** | No §1 / §2 / §3 / MEMORY row covers the question. | **Proceed to Step 3** (normal council flow). |

### Step 3 — Pre-framing context injection
For bucket E only: when framing the question for the 5 advisors (existing
step 1 of the skill), append a "Prior art" section listing:
- Any §2 `Ox` items that are tangentially related (so advisors don't
  re-raise them as "new").
- Any §1 locked lessons that bound the space (so advisors don't propose
  solutions that contradict locks).
- The 2-3 most-related §3 rows by keyword match.

This prevents advisors from re-deriving settled ground and focuses their
work on the novel part.

### Step 4 — Post-verdict update
If a new session produced a settled verdict:
- Append a row to §3 (new # continues the sequence).
- If it closes an `Ox`: move to §1 and leave `[closed <date>]` in §2.
- If it opens a new unresolved item: add a new `Ox` row with the next free
  ID (never reuse; never renumber).
- If it supersedes a §1 lesson: append a superseding bullet; mark the old
  one `[SUPERSEDED <date>]`. Do not rewrite.

**Closure-receipt gate (CI-enforced).** Every new `[closed <date>]` row
in §2 must cite at least one file under `scripts/`, `artifacts/`, or
`tests/` that exists on disk. `tests/test_closure_receipts.py` fails CI
on any closure that ships without receipts. Run
`python scripts/check_closure_receipts.py --report-only` for the
current audit. Three pre-existing rows (O1, O15, O21) are
grandfathered in `BASELINE_UNVERIFIED` because their evidence lives in
repo-root files (`pool_hist_results.json`, `ANALYSIS_O21_*.md`) or
because the closure is a blocked-on-data formalization with nothing to
serialize. Do NOT extend the baseline to silence new failures.

**Stale-citation gate (CI-enforced).** Every path token under
`scripts/`, `tests/`, `src/`, `artifacts/`, `data/`, or `configs/`
cited in this file or in `MEMORY.md` must exist on disk.
`tests/test_lesson_citations.py` fails CI on any new broken citation.
Run `python scripts/check_lesson_citations.py --report-only` for the
current audit. Two pre-existing rotted citations are grandfathered in
`BASELINE_BROKEN` (the deleted `feature_selection.py` module from D4
and the missing `artifacts/backtest_runs/mc_pool_backtest_20260415_*.txt`
log from D16/O23). Fix new failures by either restoring the file or
updating the citation to point at where the evidence now lives — do
NOT extend the baseline.

### What NOT to skip the council for
- Bucket E (genuinely novel) → run the council.
- Bucket A with explicit new evidence from the user → run the council.
- Bucket D with explicit new evidence from the user → run the council.

### What TO skip the council for
- Bucket A without new evidence → cite §3 row and stop.
- Bucket B without challenge to the gate → cite §2 O`<n>` and offer to help
  execute.
- Bucket C → cite the blocker and stop.
- Bucket D without new evidence → cite MEMORY row and stop.

---

## Update rule

When a new council session produces a settled verdict, append a row to §3
with date + one-line verdict. If it also closes or supersedes an item in §2,
move that item to §1 (lessons) or mark `[closed <date>]`. Do not edit old
entries — append, don't rewrite. Same norms as MEMORY.md.
